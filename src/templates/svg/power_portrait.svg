<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" width='210mm' height='297mm' viewBox="0 0 794 1123">
  <!-- A4 Portrait: 210mm x 297mm -->
  <!-- Template: Power Consumption Analysis Report (Vertical) - IMPROVED VERSION -->

  <!-- ========================================= -->
  <!-- DEFINITIONS (Styles, Gradients, Filters) -->
  <!-- ========================================= -->
  <defs>
    <style type="text/css">
      @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&amp;display=swap');

      .metric-title { font-family: 'Inter', sans-serif; font-size: 10px; font-weight: 600; fill: {{metric_title_color}}; text-transform: uppercase; letter-spacing: 0.05em; }
      .metric-value { font-family: 'Inter', sans-serif; font-size: 22px; font-weight: 700; fill: {{metric_value_color}}; }
      .metric-unit { font-family: 'Inter', sans-serif; font-size: 11px; fill: {{metric_title_color}}; }
      .section-header { font-family: 'Inter', sans-serif; font-size: 14px; font-weight: 600; fill: {{section_header_color}}; }
      .table-header { font-family: 'Inter', sans-serif; font-size: 10px; font-weight: 600; fill: {{table_header_color}}; text-transform: uppercase; }
      .table-text { font-family: 'Inter', sans-serif; font-size: 11px; fill: {{table_text_color}}; }
      .table-small { font-family: 'Inter', sans-serif; font-size: 9px; fill: {{table_header_color}}; }

      .card { fill: #ffffff; stroke: {{card_border_color}}; stroke-width: 1; }
      .card-elevated { fill: #ffffff; stroke: {{card_border_color}}; stroke-width: 1; filter: url(#dropShadow); }
      .card-temperature { fill: {{accent_secondary}}; stroke: {{card_border_color}}; stroke-width: 1.5; }
      .card-humidity { fill: {{accent_secondary}}; stroke: {{card_border_color}}; stroke-width: 1.5; }
      .card-sound { fill: {{accent_secondary}}; stroke: {{card_border_color}}; stroke-width: 1.5; }
      .card-power { fill: {{accent_secondary}}; stroke: {{card_border_color}}; stroke-width: 1.5; }
      .card-consumption { fill: {{accent_secondary}}; stroke: {{card_border_color}}; stroke-width: 1.5; }
      .card-details { fill: {{accent_secondary}}; stroke: {{card_border_color}}; stroke-width: 1.5; }
      .divider { stroke: {{card_border_color}}; stroke-width: 0.8; }

      /* Animation classes */
      .fade-in { opacity: 0; animation: fadeIn 0.5s ease-out forwards; }
      @keyframes fadeIn { to { opacity: 1; } }
    </style>

    <!-- ========================================= -->
    <!-- FILTERS -->
    <!-- ========================================= -->

    <!-- Drop shadow for card elevation -->
    <filter id="dropShadow" x="-10%" y="-10%" width="120%" height="140%">
      <feDropShadow dx="0" dy="2" stdDeviation="4" flood-color="#000000" flood-opacity="0.12"/>
    </filter>

    <!-- Subtle inner glow for progress bar -->
    <filter id="innerGlow" x="-20%" y="-20%" width="140%" height="140%">
      <feGaussianBlur stdDeviation="2" result="blur"/>
      <feComposite in="SourceGraphic" in2="blur" operator="over"/>
    </filter>

    <!-- ========================================= -->
    <!-- GRADIENTS -->
    <!-- ========================================= -->

    <!-- Glass overlay effect for progress bars -->
    <linearGradient id="glassOverlay" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#FFFFFF" stop-opacity="0.3"/>
      <stop offset="45%" stop-color="#FFFFFF" stop-opacity="0.08"/>
      <stop offset="55%" stop-color="#000000" stop-opacity="0.03"/>
      <stop offset="100%" stop-color="#000000" stop-opacity="0.12"/>
    </linearGradient>

    <!-- Phase gradients matching chart colors -->
    <!-- Ch1 Gradient (Blue) -->
    <linearGradient id="ch1Gradient" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#60A5FA"/>
      <stop offset="50%" stop-color="#3B82F6"/>
      <stop offset="100%" stop-color="#2563EB"/>
    </linearGradient>

    <!-- Ch2 Gradient (Green) -->
    <linearGradient id="ch2Gradient" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#34D399"/>
      <stop offset="50%" stop-color="#10B981"/>
      <stop offset="100%" stop-color="#059669"/>
    </linearGradient>

    <!-- Ch3 Gradient (Amber) -->
    <linearGradient id="ch3Gradient" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#FBBF24"/>
      <stop offset="50%" stop-color="#F59E0B"/>
      <stop offset="100%" stop-color="#D97706"/>
    </linearGradient>

    <!-- Background track gradient -->
    <linearGradient id="trackGradient" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#F3F4F6"/>
      <stop offset="50%" stop-color="#E5E7EB"/>
      <stop offset="100%" stop-color="#D1D5DB"/>
    </linearGradient>

  </defs>

  <!-- Background -->
  <rect width="794" height="1123" fill="#FFFFFF"/>

  <!-- ========================================= -->
  <!-- HEADER SECTION -->
  <!-- ========================================= -->
  <g id="header">
    <rect width="794" height="140" fill="{{header_bg}}"/>
    <image href="{{logo_url}}" x="35" y="15" width="105" height="105" preserveAspectRatio="xMidYMid meet"/>
    <text x="160" y="65" fill="{{header_text_color}}" font-size="24" font-family="Inter, Arial, sans-serif" font-weight="700">
      {{header_title}}
    </text>
    <text x="160" y="95" fill="{{header_subtitle_color}}" font-size="16" font-family="Inter, Arial, sans-serif" font-weight="400">
      {{header_subtitle}}
    </text>
  </g>

  <!-- ========================================= -->
  <!-- REPORT INFO BAR (Improved) -->
  <!-- ========================================= -->
  <g id="report-info" role="region" aria-label="Report Information">
    <rect x="30" y="160" width="734" height="48" class="card-elevated" rx="6" ry="6"/>

    <!-- Building Name -->
    <g transform="translate(50, 160)">
      <text y="18" font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#6B7280" letter-spacing="0.5">NOMBRE DEL EDIFICIO</text>
      <text y="36" font-family="Inter, sans-serif" font-size="13" font-weight="600" fill="#111827">{{building_name}}</text>
    </g>

    <!-- Date Range -->
    <g transform="translate(280, 160)">
      <text y="18" font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#6B7280" letter-spacing="0.5">PERIODO DEL INFORME</text>
      <text y="36" font-family="Inter, sans-serif" font-size="13" font-weight="600" fill="#111827">{{date_range}}</text>
    </g>

    <!-- Total Sensors -->
    <g transform="translate(500, 160)">
      <text y="18" font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#6B7280" letter-spacing="0.5">TOTAL SENSORES</text>
      <text y="38" font-family="Inter, sans-serif" font-size="18" font-weight="700" fill="#111827">{{total_sensors}}</text>
    </g>

    <!-- Active Sensors -->
    <g transform="translate(640, 160)">
      <text y="18" font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#6B7280" letter-spacing="0.5">ACTIVOS</text>
      <text y="38" font-family="Inter, sans-serif" font-size="18" font-weight="700" fill="{{accent_primary}}">{{active_sensors}}</text>
    </g>
  </g>

  <!-- ========================================= -->
  <!-- THREE-PHASE LOAD BALANCE (Improved) -->
  <!-- ========================================= -->
  <g id="phase-balance-indicator" role="figure" aria-label="Three-Phase Load Balance">
    <!-- Card Container with Elevation -->
    <rect x="30" y="220" width="734" height="150" class="card-elevated" rx="8" ry="8"/>

    <!-- Section Header with Icon -->
    <g id="balance-header" transform="translate(50, 248)">
      <text font-family="Inter, sans-serif" font-size="14" font-weight="700" fill="#1F2937">
        <tspan fill="#F59E0B">⚡</tspan>
        <tspan dx="6">Three-Phase Load Balance</tspan>
      </text>
    </g>

    <!-- Phase Bars Container -->
    <g id="phase-bars" transform="translate(50, 265)">

      <!-- Phase R (Blue) -->
      <g id="phase-ch1">
        <text x="0" y="17" font-family="Inter, sans-serif" font-size="12" font-weight="700" fill="#1F2937">Fase R</text>

        <!-- Background Track -->
        <rect x="45" y="2" width="200" height="24" fill="url(#trackGradient)" rx="12" opacity="0.6"/>

        <!-- Progress Bar with Animation -->
        <rect x="45" y="2" width="0" height="24" fill="url(#ch1Gradient)" rx="12">
          <animate
            attributeName="width"
            from="0" to="{{phase_f1_bar_width}}"
            dur="0.8s"
            fill="freeze"
            calcMode="spline"
            keySplines="0.25 0.1 0.25 1"
          />
        </rect>

        <!-- Glass Overlay -->
        <rect x="45" y="2" width="200" height="24" fill="url(#glassOverlay)" rx="12" pointer-events="none"/>

        <!-- Percentage Label -->
        <text x="255" y="17" font-family="Inter, sans-serif" font-size="13" font-weight="700" fill="#1F2937">{{phase_f1_percentage}}%</text>
      </g>

      <!-- Phase S (Green) -->
      <g id="phase-ch2" transform="translate(0, 35)">
        <text x="0" y="17" font-family="Inter, sans-serif" font-size="12" font-weight="700" fill="#1F2937">Fase S</text>

        <!-- Background Track -->
        <rect x="45" y="2" width="200" height="24" fill="url(#trackGradient)" rx="12" opacity="0.6"/>

        <!-- Progress Bar with Animation -->
        <rect x="45" y="2" width="0" height="24" fill="url(#ch2Gradient)" rx="12">
          <animate
            attributeName="width"
            from="0" to="{{phase_f2_bar_width}}"
            dur="0.8s"
            begin="0.15s"
            fill="freeze"
            calcMode="spline"
            keySplines="0.25 0.1 0.25 1"
          />
        </rect>

        <!-- Glass Overlay -->
        <rect x="45" y="2" width="200" height="24" fill="url(#glassOverlay)" rx="12" pointer-events="none"/>

        <!-- Percentage Label -->
        <text x="255" y="17" font-family="Inter, sans-serif" font-size="13" font-weight="700" fill="#1F2937">{{phase_f2_percentage}}%</text>
      </g>

      <!-- Phase T (Amber) -->
      <g id="phase-ch3" transform="translate(0, 70)">
        <text x="0" y="17" font-family="Inter, sans-serif" font-size="12" font-weight="700" fill="#1F2937">Fase T</text>

        <!-- Background Track -->
        <rect x="45" y="2" width="200" height="24" fill="url(#trackGradient)" rx="12" opacity="0.6"/>

        <!-- Progress Bar with Animation -->
        <rect x="45" y="2" width="0" height="24" fill="url(#ch3Gradient)" rx="12">
          <animate
            attributeName="width"
            from="0" to="{{phase_f3_bar_width}}"
            dur="0.8s"
            begin="0.3s"
            fill="freeze"
            calcMode="spline"
            keySplines="0.25 0.1 0.25 1"
          />
        </rect>

        <!-- Glass Overlay -->
        <rect x="45" y="2" width="200" height="24" fill="url(#glassOverlay)" rx="12" pointer-events="none"/>

        <!-- Percentage Label -->
        <text x="255" y="17" font-family="Inter, sans-serif" font-size="13" font-weight="700" fill="#1F2937">{{phase_f3_percentage}}%</text>
      </g>

      <!-- Balance Status Card -->
      <g id="balance-status" transform="translate(360, 0)">
        <!-- Status Card Background -->
        <rect x="-10" y="-5" width="300" height="75" rx="8" fill="#F9FAFB" stroke="#E5E7EB" stroke-width="1"/>

        <!-- Title -->
        <text x="0" y="12" font-family="Inter, sans-serif" font-size="11" font-weight="600" fill="#6B7280" letter-spacing="0.5">Balance Status</text>

        <!-- Status Indicator with Badge -->
        <g transform="translate(0, 25)">
          <circle cx="12" cy="0" r="10" fill="{{phase_balance_status_color}}" opacity="0.2"/>
          <circle cx="12" cy="0" r="6" fill="{{phase_balance_status_color}}"/>
          <text x="30" y="5" font-family="Inter, sans-serif" font-size="14" font-weight="700" fill="#111827">{{phase_balance_status_text}}</text>
        </g>

        <!-- Deviation Info -->
        <g transform="translate(0, 50)">
          <text x="0" y="0" font-family="Inter, sans-serif" font-size="10" font-weight="600" fill="#6B7280">Deviation: </text>
          <text x="65" y="0" font-family="Inter, sans-serif" font-size="12" font-weight="700" fill="#DC2626">{{phase_balance_deviation}}%</text>
        </g>
      </g>
    </g>
  </g>

  <!-- ========================================= -->
  <!-- POWER CONSUMPTION CHART SECTION (Improved) -->
  <!-- ========================================= -->
  <g id="chart-section">
    <text x="30" y="390" class="section-header">Average Consumption per Channel</text>

    <rect x="30" y="400" width="734" height="280" class="card-elevated" rx="8"/>

    <!-- Two Column Layout: Left 40% (Electric Image) | Right 60% (Chart + Legend) -->

    <!-- LEFT COLUMN: Electric Image (40% = ~294px) -->
    <g id="electric-section" transform="translate(50, 415)">
      <!-- Electric Image -->
      <image href="{{electric_url}}" x="30" y="10" width="120" height="240" preserveAspectRatio="xMidYMid meet"/>

      <!-- Consumption values on the right side with colored boxes -->

      <!-- Max consumption (top - HIGH level = red) -->
      <g id="max-consumption-display" transform="translate(160, 45)">
        <rect x="-8" y="-18" width="90" height="50" rx="6" fill="#FEF2F2" stroke="#FECACA" stroke-width="0.5"/>
        <text x="0" y="0" font-family="Inter, sans-serif" font-size="9" font-weight="700" fill="#DC2626" letter-spacing="0.5">MAX</text>
        <text x="0" y="18" font-family="Inter, sans-serif" font-size="18" font-weight="700" fill="#DC2626">{{overall_max_consumption}}</text>
        <text x="0" y="32" font-family="Inter, sans-serif" font-size="10" font-weight="600" fill="#F87171">{{max_consumption_unit}}</text>
      </g>

      <!-- Average consumption (middle - NORMAL level = yellow/amber) -->
      <g id="avg-consumption-display" transform="translate(160, 125)">
        <rect x="-8" y="-18" width="90" height="50" rx="6" fill="#FFFBEB" stroke="#FDE68A" stroke-width="0.5"/>
        <text x="0" y="0" font-family="Inter, sans-serif" font-size="9" font-weight="700" fill="#D97706" letter-spacing="0.5">AVG</text>
        <text x="0" y="18" font-family="Inter, sans-serif" font-size="16" font-weight="700" fill="#D97706">{{overall_avg_consumption}}</text>
        <text x="0" y="32" font-family="Inter, sans-serif" font-size="10" font-weight="600" fill="#F59E0B">{{avg_consumption_unit}}</text>
      </g>

      <!-- Min consumption (bottom - LOW level = green) -->
      <g id="min-consumption-display" transform="translate(160, 205)">
        <rect x="-8" y="-18" width="90" height="50" rx="6" fill="#ECFDF5" stroke="#A7F3D0" stroke-width="0.5"/>
        <text x="0" y="0" font-family="Inter, sans-serif" font-size="9" font-weight="700" fill="#059669" letter-spacing="0.5">MIN</text>
        <text x="0" y="18" font-family="Inter, sans-serif" font-size="18" font-weight="700" fill="#059669">{{overall_min_consumption}}</text>
        <text x="0" y="32" font-family="Inter, sans-serif" font-size="10" font-weight="600" fill="#10B981">{{min_consumption_unit}}</text>
      </g>
    </g>

    <!-- RIGHT COLUMN: Chart + Legend (60% = ~440px) -->
    <g id="chart-column" transform="translate(334, 410)">
      <!-- Chart Legend with Badge Style -->
      <g id="chart-legend" font-family="Inter, sans-serif">
        <!-- Fase R Legend -->
        <g transform="translate(0, 0)">
          <rect x="-4" y="-11" width="100" height="22" rx="11" fill="#EFF6FF" stroke="#BFDBFE" stroke-width="0.5"/>
          <rect x="4" y="-3" width="8" height="8" rx="2" fill="url(#ch1Gradient)"/>
          <text x="18" y="4" font-size="10" font-weight="600" fill="#1E40AF">Fase R</text>
        </g>

        <!-- Fase S Legend -->
        <g transform="translate(115, 0)">
          <rect x="-4" y="-11" width="100" height="22" rx="11" fill="#ECFDF5" stroke="#A7F3D0" stroke-width="0.5"/>
          <rect x="4" y="-3" width="8" height="8" rx="2" fill="url(#ch2Gradient)"/>
          <text x="18" y="4" font-size="10" font-weight="600" fill="#065F46">Fase S</text>
        </g>

        <!-- Fase T Legend -->
        <g transform="translate(230, 0)">
          <rect x="-4" y="-11" width="100" height="22" rx="11" fill="#FEF3C7" stroke="#FDE68A" stroke-width="0.5"/>
          <rect x="4" y="-3" width="8" height="8" rx="2" fill="url(#ch3Gradient)"/>
          <text x="18" y="4" font-size="10" font-weight="600" fill="#92400E">Fase T</text>
        </g>
      </g>

      <!-- Chart Placeholder -->
      <foreignObject x="0" y="25" width="420" height="230">
        <div xmlns="http://www.w3.org/1999/xhtml" id="chart-container" style="width:100%; height:100%;">
          <canvas id="consumption-comparison-chart"></canvas>
        </div>
      </foreignObject>
    </g>
  </g>

  <!-- ========================================= -->
  <!-- CONSUMPTION DETAILS SECTION (Improved) -->
  <!-- ========================================= -->
  <g id="consumption-details">
    <text x="30" y="710" class="section-header">Consumption Details</text>

    <!-- Total Consumption Details Card -->
    <rect x="30" y="720" width="734" height="145" class="card-elevated" rx="8"/>

    <!-- Card Header -->
    <g transform="translate(45, 740)">
      <text font-family="Inter, sans-serif" font-size="11" font-weight="700" fill="#6B7280" letter-spacing="0.5">
        <tspan fill="#3B82F6">📊</tspan>
        <tspan dx="4">Total Consumption Summary</tspan>
      </text>
    </g>

    <!-- 3 Column Layout: Amperes | kWh | Estimated Cost -->

    <!-- Amperes Column -->
    <g id="consumption-amperes" transform="translate(60, 780)">
      <rect x="-15" y="-5" width="180" height="70" rx="6" fill="#F9FAFB" stroke="#E5E7EB" stroke-width="0.5"/>
      <text y="0" fill="#6B7280" font-family="Inter, sans-serif" font-size="10" font-weight="600" letter-spacing="0.5">Total Amperes</text>
      <text y="30" fill="#111827" font-family="Inter, sans-serif" font-size="24" font-weight="800">{{total_amperes}}</text>
      <text y="48" fill="#9CA3AF" font-family="Inter, sans-serif" font-size="12" font-weight="600">A</text>
    </g>

    <!-- kWh Column -->
    <g id="consumption-kwh" transform="translate(310, 780)">
      <rect x="-15" y="-5" width="180" height="70" rx="6" fill="#F9FAFB" stroke="#E5E7EB" stroke-width="0.5"/>
      <text y="0" fill="#6B7280" font-family="Inter, sans-serif" font-size="10" font-weight="600" letter-spacing="0.5">Total Energy</text>
      <text y="30" fill="#111827" font-family="Inter, sans-serif" font-size="24" font-weight="800">{{total_kwh}}</text>
      <text y="48" fill="#9CA3AF" font-family="Inter, sans-serif" font-size="12" font-weight="600">kWh</text>
    </g>

    <!-- Cost Column -->
    <g id="consumption-cost" transform="translate(560, 780)">
      <rect x="-15" y="-5" width="180" height="70" rx="6" fill="#ECFDF5" stroke="#A7F3D0" stroke-width="0.5"/>
      <text y="0" fill="#6B7280" font-family="Inter, sans-serif" font-size="10" font-weight="600" letter-spacing="0.5">Estimated Cost (Spain)</text>
      <text y="30" fill="#059669" font-family="Inter, sans-serif" font-size="24" font-weight="800">€{{total_cost}}</text>
      <text y="48" fill="#10B981" font-family="Inter, sans-serif" font-size="12" font-weight="600">EUR</text>
    </g>
  </g>

  <!-- ========================================= -->
  <!-- PEAK LOAD TIMES SECTION (Improved) -->
  <!-- ========================================= -->
  <g id="peak-loads">
    <text x="30" y="895" class="section-header">Peak Load Times &amp; Values</text>

    <!-- Card with light grayscale background -->
    <rect x="30" y="905" width="734" height="115" class="card-elevated" rx="8"/>
    <rect x="30" y="905" width="734" height="115" rx="8" fill="#F9FAFB" opacity="0.8"/>

    <!-- Table Title -->
    <g transform="translate(45, 925)">
      <text font-family="Inter, sans-serif" font-size="11" font-weight="700" fill="#DC2626">
        <tspan fill="#EF4444">⚡</tspan>
        <tspan dx="4">Peak Load Events</tspan>
        <tspan dx="6" font-size="9" font-weight="500" fill="#6B7280">(Top 2)</tspan>
      </text>
    </g>

    <!-- Table Header -->
    <g transform="translate(0, 950)" font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#64748B">
      <text x="45">Rank</text>
      <text x="100">Sensor</text>
      <text x="240">Channel</text>
      <text x="360">Peak Value</text>
      <text x="500">Time</text>
      <text x="660">Power (kW)</text>
    </g>

    <line x1="45" y1="958" x2="749" y2="958" stroke="#CBD5E1" stroke-width="0.8" opacity="0.6"/>

    <!-- Peak Row 1 -->
    <g transform="translate(0, 978)" font-family="Inter, sans-serif">
      <rect x="40" y="-12" width="24" height="18" rx="4" fill="#EF4444" opacity="0.2"/>
      <text x="52" fill="#DC2626" font-size="11" font-weight="700" text-anchor="middle">#1</text>
      <text x="100" fill="#1F2937" font-size="11">{{peak_1_sensor}}</text>
      <text x="240" fill="#1F2937" font-size="11">{{peak_1_channel}}</text>
      <text x="360" fill="#111827" font-size="13" font-weight="700">{{peak_1_value}}<tspan fill="#6B7280" font-size="10">A</tspan></text>
      <text x="500" fill="#6B7280" font-size="10">{{peak_1_time}}</text>
      <text x="660" fill="#DC2626" font-size="12" font-weight="600">{{peak_1_power}} kW</text>
    </g>

    <!-- Peak Row 2 -->
    <g transform="translate(0, 1000)" font-family="Inter, sans-serif">
      <rect x="40" y="-12" width="24" height="18" rx="4" fill="#EF4444" opacity="0.15"/>
      <text x="52" fill="#EF4444" font-size="11" font-weight="600" text-anchor="middle">#2</text>
      <text x="100" fill="#1F2937" font-size="11">{{peak_2_sensor}}</text>
      <text x="240" fill="#1F2937" font-size="11">{{peak_2_channel}}</text>
      <text x="360" fill="#111827" font-size="13" font-weight="700">{{peak_2_value}}<tspan fill="#6B7280" font-size="10">A</tspan></text>
      <text x="500" fill="#6B7280" font-size="10">{{peak_2_time}}</text>
      <text x="660" fill="#DC2626" font-size="12" font-weight="600">{{peak_2_power}} kW</text>
    </g>
  </g>

  <!-- ========================================= -->
  <!-- FOOTER SECTION -->
  <!-- ========================================= -->
  <g id="footer">
    <line x1="30" y1="1050" x2="764" y2="1050" stroke="{{footer_bg}}" stroke-width="2"/>
    <rect y="1058" width="794" height="65" fill="{{footer_bg}}"/>
    <text x="40" y="1090" fill="{{footer_text_color}}" font-size="12" font-family="Inter, Arial, sans-serif">
      {{footer_text}}
    </text>
    <text x="754" y="1090" fill="{{footer_date_color}}" font-size="12" font-family="Inter, Arial, sans-serif" text-anchor="end">
      {{generation_date}}
    </text>
  </g>
</svg>
