<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" width='210mm' height='297mm' viewBox="0 0 794 1123">
  <!-- A4 Portrait: 210mm x 297mm -->
  <!-- Template: Sound Levels - Noise Pollution Analysis Report (Vertical) - IMPROVED VERSION -->

  <!-- ========================================= -->
  <!-- DEFINITIONS (Styles, Gradients, Filters) -->
  <!-- ========================================= -->
  <defs>
    <style type="text/css">
      @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&amp;display=swap');

      .metric-title { font-family: 'Inter', sans-serif; font-size: 10px; font-weight: 600; fill: {{metric_title_color}}; text-transform: uppercase; letter-spacing: 0.05em; }
      .metric-value { font-family: 'Inter', sans-serif; font-size: 22px; font-weight: 700; fill: {{metric_value_color}}; }
      .metric-unit { font-family: 'Inter', sans-serif; font-size: 11px; fill: {{metric_title_color}}; }
      .section-header { font-family: 'Inter', sans-serif; font-size: 14px; font-weight: 600; fill: {{section_header_color}}; }
      .table-header { font-family: 'Inter', sans-serif; font-size: 10px; font-weight: 600; fill: {{table_header_color}}; text-transform: uppercase; }
      .table-text { font-family: 'Inter', sans-serif; font-size: 11px; fill: {{table_text_color}}; }
      .table-small { font-family: 'Inter', sans-serif; font-size: 9px; fill: {{table_header_color}}; }

      .card { fill: #ffffff; stroke: {{card_border_color}}; stroke-width: 1; }
      .card-elevated { fill: #ffffff; stroke: {{card_border_color}}; stroke-width: 1; filter: url(#dropShadow); }
      .card-noise { fill: {{accent_secondary}}; stroke: {{card_border_color}}; stroke-width: 1.5; }
      .card-quiet { fill: {{accent_secondary}}; stroke: {{card_border_color}}; stroke-width: 1.5; }
      .divider { stroke: {{card_border_color}}; stroke-width: 0.8; }
    </style>

    <!-- Drop shadow for card elevation -->
    <filter id="dropShadow" x="-10%" y="-10%" width="120%" height="140%">
      <feDropShadow dx="0" dy="2" stdDeviation="4" flood-color="#000000" flood-opacity="0.12"/>
    </filter>

    <!-- Glass overlay effect -->
    <linearGradient id="glassOverlay" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#FFFFFF" stop-opacity="0.3"/>
      <stop offset="45%" stop-color="#FFFFFF" stop-opacity="0.08"/>
      <stop offset="55%" stop-color="#000000" stop-opacity="0.03"/>
      <stop offset="100%" stop-color="#000000" stop-opacity="0.12"/>
    </linearGradient>

    <!-- Noise level gradients -->
    <linearGradient id="lowNoiseGradient" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#34D399"/>
      <stop offset="50%" stop-color="#10B981"/>
      <stop offset="100%" stop-color="#059669"/>
    </linearGradient>

    <linearGradient id="normalNoiseGradient" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#FBBF24"/>
      <stop offset="50%" stop-color="#F59E0B"/>
      <stop offset="100%" stop-color="#D97706"/>
    </linearGradient>

    <linearGradient id="highNoiseGradient" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#F87171"/>
      <stop offset="50%" stop-color="#EF4444"/>
      <stop offset="100%" stop-color="#DC2626"/>
    </linearGradient>

  </defs>

  <!-- Background -->
  <rect width="794" height="1123" fill="#FFFFFF"/>

  <!-- ========================================= -->
  <!-- HEADER SECTION -->
  <!-- ========================================= -->
  <g id="header">
    <rect width="794" height="140" fill="{{header_bg}}"/>
    <image href="{{logo_url}}" x="35" y="15" width="105" height="105" preserveAspectRatio="xMidYMid meet"/>
    <text x="160" y="65" fill="{{header_text_color}}" font-size="24" font-family="Inter, Arial, sans-serif" font-weight="700">
      {{header_title}}
    </text>
    <text x="160" y="95" fill="{{header_subtitle_color}}" font-size="16" font-family="Inter, Arial, sans-serif" font-weight="400">
      {{header_subtitle}}
    </text>
  </g>

  <!-- ========================================= -->
  <!-- REPORT INFO BAR -->
  <!-- ========================================= -->
  <g id="report-info">
    <rect x="30" y="145" width="734" height="48" class="card-elevated" rx="6" ry="6"/>

    <!-- Building Name -->
    <g transform="translate(50, 145)">
      <text y="18" font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#6B7280" letter-spacing="0.5">{{label_building_name}}</text>
      <text y="36" font-family="Inter, sans-serif" font-size="13" font-weight="600" fill="#111827">{{building_name}}</text>
    </g>

    <!-- Date Range -->
    <g transform="translate(280, 145)">
      <text y="18" font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#6B7280" letter-spacing="0.5">{{label_report_period}}</text>
      <text y="36" font-family="Inter, sans-serif" font-size="13" font-weight="600" fill="#111827">{{date_range}}</text>
    </g>

    <!-- Total Sensors -->
    <g transform="translate(500, 145)">
      <text y="18" font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#6B7280" letter-spacing="0.5">{{label_total_sensors}}</text>
      <text y="38" font-family="Inter, sans-serif" font-size="18" font-weight="700" fill="#111827">{{total_sensors}}</text>
    </g>

    <!-- Active Sensors -->
    <g transform="translate(640, 145)">
      <text y="18" font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#6B7280" letter-spacing="0.5">{{label_active}}</text>
      <text y="38" font-family="Inter, sans-serif" font-size="18" font-weight="700" fill="{{accent_primary}}">{{active_sensors}}</text>
    </g>
  </g>

  <!-- ========================================= -->
  <!-- REPORT OVERVIEW -->
  <!-- ========================================= -->
  <g id="report-description">
    <text x="30" y="220" class="section-header">{{label_report_overview}}</text>

    <rect x="30" y="230" width="734" height="60" class="card-elevated" rx="8" ry="8"/>

    <!-- Description Text -->
    <foreignObject x="50" y="242" width="694" height="42">
      <div xmlns="http://www.w3.org/1999/xhtml" style="font-family: 'Inter', sans-serif; font-size: 10px; line-height: 1.5; color: #4B5563; text-align: justify;">
        {{report_description}}
      </div>
    </foreignObject>
  </g>

  <!-- ========================================= -->
  <!-- NOISE DISTRIBUTION INDICATOR -->
  <!-- ========================================= -->
  <g id="noise-comfort-indicator">
    <!-- Card Container -->
    <rect x="30" y="310" width="734" height="125" class="card-elevated" rx="8" ry="8"/>

    <!-- LEFT COLUMN: Key Metrics -->
    <g id="key-metrics-column">
      <!-- Section Header -->
      <g transform="translate(50, 338)">
        <text font-family="Inter, sans-serif" font-size="14" font-weight="700" fill="#1F2937">
          {{label_key_metrics}}
        </text>
      </g>

      <!-- Comfort Metric Card -->
      <g transform="translate(50, 355)">
        <rect x="0" y="0" width="145" height="68" rx="8" fill="#ECFDF5" stroke="#A7F3D0" stroke-width="1"/>
        <!-- Icon -->
        <circle cx="24" cy="34" r="16" fill="#D1FAE5"/>
        <text x="24" y="39" font-family="Inter, sans-serif" font-size="14" text-anchor="middle" fill="#10B981">&#10003;</text>
        <!-- Content -->
        <text x="52" y="22" font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#065F46" letter-spacing="0.3">{{label_comfort}}</text>
        <text x="52" y="46" font-family="Inter, sans-serif" font-size="26" font-weight="800" fill="#10B981">{{comfort_percentage}}<tspan font-size="14" font-weight="600">%</tspan></text>
        <text x="52" y="60" font-family="Inter, sans-serif" font-size="8" fill="#34D399">{{label_ideal_zone}}</text>
      </g>

      <!-- Critical Metric Card -->
      <g transform="translate(205, 355)">
        <rect x="0" y="0" width="145" height="68" rx="8" fill="#FEF2F2" stroke="#FECACA" stroke-width="1"/>
        <!-- Icon -->
        <circle cx="24" cy="34" r="16" fill="#FEE2E2"/>
        <text x="24" y="39" font-family="Inter, sans-serif" font-size="14" text-anchor="middle" fill="#DC2626">!</text>
        <!-- Content -->
        <text x="52" y="22" font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#991B1B" letter-spacing="0.3">{{label_critical}}</text>
        <text x="52" y="46" font-family="Inter, sans-serif" font-size="26" font-weight="800" fill="#DC2626">{{critical_percentage}}<tspan font-size="14" font-weight="600">%</tspan></text>
        <text x="52" y="60" font-family="Inter, sans-serif" font-size="8" fill="#F87171">{{label_above_threshold}}</text>
      </g>
    </g>

    <!-- RIGHT COLUMN: Noise Distribution Donut Chart -->
    <g id="noise-distribution-chart">
      <!-- Section Header -->
      <g transform="translate(400, 338)">
        <text font-family="Inter, sans-serif" font-size="14" font-weight="700" fill="#1F2937">
          {{label_noise_distribution}}
        </text>
      </g>

      <!-- Donut Chart -->
      <g transform="translate(540, 390)">
        <!-- Background Circle -->
        <circle cx="0" cy="0" r="31.5" fill="none" stroke="#F3F4F6" stroke-width="15"/>

        <!-- Quiet Segment (Green) - starts at top (-90deg) -->
        <circle
          cx="0" cy="0" r="31.5"
          fill="none"
          stroke="#10B981"
          stroke-width="15"
          stroke-dasharray="{{quiet_arc_length}} 198"
          stroke-dashoffset="0"
          transform="rotate(-90)"
        />

        <!-- Normal Segment (Yellow) - starts after quiet -->
        <circle
          cx="0" cy="0" r="31.5"
          fill="none"
          stroke="#F59E0B"
          stroke-width="15"
          stroke-dasharray="{{normal_arc_length}} 198"
          stroke-dashoffset="{{normal_arc_offset}}"
          transform="rotate(-90)"
        />

        <!-- Loud Segment (Red) - starts after normal -->
        <circle
          cx="0" cy="0" r="31.5"
          fill="none"
          stroke="#EF4444"
          stroke-width="15"
          stroke-dasharray="{{loud_arc_length}} 198"
          stroke-dashoffset="{{loud_arc_offset}}"
          transform="rotate(-90)"
        />

        <!-- Center Text -->
        <text x="0" y="-5" font-family="Inter, sans-serif" font-size="10" font-weight="600" fill="#6B7280" text-anchor="middle">{{label_total}}</text>
        <text x="0" y="12" font-family="Inter, sans-serif" font-size="16" font-weight="700" fill="#1F2937" text-anchor="middle">100%</text>
      </g>

      <!-- Legend -->
      <g transform="translate(620, 348)" font-family="Inter, sans-serif">
        <!-- Quiet -->
        <g transform="translate(0, 0)">
          <rect x="0" y="0" width="10" height="10" rx="2" fill="#10B981"/>
          <text x="16" y="9" font-size="9" font-weight="600" fill="#065F46">{{label_quiet}}</text>
          <text x="60" y="9" font-size="9" font-weight="700" fill="#1F2937">{{quiet_percentage}}%</text>
        </g>

        <!-- Normal -->
        <g transform="translate(0, 20)">
          <rect x="0" y="0" width="10" height="10" rx="2" fill="#F59E0B"/>
          <text x="16" y="9" font-size="9" font-weight="600" fill="#92400E">{{label_normal}}</text>
          <text x="60" y="9" font-size="9" font-weight="700" fill="#1F2937">{{normal_percentage}}%</text>
        </g>

        <!-- Loud -->
        <g transform="translate(0, 40)">
          <rect x="0" y="0" width="10" height="10" rx="2" fill="#EF4444"/>
          <text x="16" y="9" font-size="9" font-weight="600" fill="#991B1B">{{label_loud}}</text>
          <text x="60" y="9" font-size="9" font-weight="700" fill="#1F2937">{{loud_percentage}}%</text>
        </g>
      </g>

      <!-- Noise Ranges Label -->
      <g transform="translate(620, 410)" font-family="Inter, sans-serif">
        <text font-size="7" fill="#9CA3AF">{{label_noise_ranges}}</text>
      </g>
    </g>
  </g>

  <!-- ========================================= -->
  <!-- NOISE ANALYSIS CHART SECTION -->
  <!-- ========================================= -->
  <g id="chart-section">
    <text x="30" y="460" class="section-header">{{label_noisiest_quietest}}</text>

    <rect x="30" y="470" width="734" height="280" class="card-elevated" rx="8"/>

    <!-- LEFT COLUMN: Sound Image -->
    <g id="sound-section" transform="translate(50, 485)">
      <image href="{{sound_url}}" x="30" y="10" width="120" height="240" preserveAspectRatio="xMidYMid meet"/>

      <!-- Max Sound Level -->
      <g transform="translate(160, 45)">
        <rect x="-8" y="-18" width="75" height="45" rx="6" fill="#FEF2F2" stroke="#FECACA" stroke-width="0.5"/>
        <text x="0" y="0" font-family="Inter, sans-serif" font-size="9" font-weight="700" fill="#DC2626" letter-spacing="0.5">MAX</text>
        <text x="0" y="18" font-family="Inter, sans-serif" font-size="18" font-weight="700" fill="#DC2626">{{overall_max_sound}}<tspan font-size="11"> dB</tspan></text>
      </g>

      <!-- Average Sound Level -->
      <g transform="translate(160, 115)">
        <rect x="-8" y="-18" width="75" height="45" rx="6" fill="#FFFBEB" stroke="#FDE68A" stroke-width="0.5"/>
        <text x="0" y="0" font-family="Inter, sans-serif" font-size="9" font-weight="700" fill="#D97706" letter-spacing="0.5">AVG</text>
        <text x="0" y="18" font-family="Inter, sans-serif" font-size="16" font-weight="700" fill="#D97706">{{overall_avg_sound}}<tspan font-size="10"> dB</tspan></text>
      </g>

      <!-- Min Sound Level -->
      <g transform="translate(160, 185)">
        <rect x="-8" y="-18" width="75" height="45" rx="6" fill="#ECFDF5" stroke="#A7F3D0" stroke-width="0.5"/>
        <text x="0" y="0" font-family="Inter, sans-serif" font-size="9" font-weight="700" fill="#10B981" letter-spacing="0.5">MIN</text>
        <text x="0" y="18" font-family="Inter, sans-serif" font-size="18" font-weight="700" fill="#10B981">{{overall_min_sound}}<tspan font-size="11"> dB</tspan></text>
      </g>
    </g>

    <!-- RIGHT COLUMN: Chart -->
    <g transform="translate(334, 480)">
      <!-- Chart Legend -->
      <g font-family="Inter, sans-serif">
        <g transform="translate(0, 0)">
          <rect width="8" height="8" rx="2" fill="#EF4444"/>
          <text x="14" y="8" font-size="9" font-weight="600" fill="#6B7280">Noisiest</text>
        </g>
        <g transform="translate(100, 0)">
          <rect width="8" height="8" rx="2" fill="#10B981"/>
          <text x="14" y="8" font-size="9" font-weight="600" fill="#6B7280">Quietest</text>
        </g>
        <g transform="translate(200, 0)">
          <line x1="0" y1="4" x2="12" y2="4" stroke="#9CA3AF" stroke-width="2" stroke-dasharray="4,2"/>
          <text x="18" y="8" font-size="9" font-weight="600" fill="#6B7280">Average</text>
        </g>
      </g>

      <!-- Chart Placeholder -->
      <foreignObject x="0" y="20" width="420" height="235">
        <div xmlns="http://www.w3.org/1999/xhtml" id="chart-container" style="width:100%; height:100%;">
          <canvas id="noise-comparison-chart"></canvas>
        </div>
      </foreignObject>
    </g>
  </g>

  <!-- ========================================= -->
  <!-- SENSOR DETAILS SECTION -->
  <!-- ========================================= -->
  <g id="noise-details">
    <text x="30" y="775" class="section-header">{{label_noise_details}}</text>

    <!-- TWO COLUMN LAYOUT -->
    <g id="noise-extremes-layout">

      <!-- LEFT CARD: NOISIEST & QUIETEST DETAILS -->
      <g id="noise-extremes-card">
        <rect x="30" y="785" width="357" height="240" class="card-elevated" rx="8"/>

        <!-- NOISIEST SECTION (Top Half) -->
        <g id="noisiest-section">
          <!-- Header with Background -->
          <rect x="30" y="785" width="357" height="35" rx="8" fill="#FEF2F2"/>
          <g transform="translate(45, 805)">
            <text font-family="Inter, sans-serif" font-size="11" font-weight="700" fill="#DC2626">
              <tspan fill="#EF4444">&#128266; </tspan>
              <tspan>NOISIEST LOCATION</tspan>
            </text>
          </g>

          <!-- Noisiest Content -->
          <g transform="translate(45, 830)">
            <!-- Sensor Name -->
            <text font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#6B7280" letter-spacing="0.5">SENSOR: <tspan font-size="12" font-weight="700" fill="#1F2937">{{noisiest_1_name}}</tspan></text>

            <!-- Metrics Row -->
            <g transform="translate(0, 25)">
              <!-- Average -->
              <g>
                <rect x="-6" y="-10" width="85" height="45" rx="5" fill="#FEF2F2" stroke="#FECACA" stroke-width="1"/>
                <text x="36" y="4" font-family="Inter, sans-serif" font-size="8" font-weight="700" fill="#DC2626" letter-spacing="0.5" text-anchor="middle">AVG</text>
                <text x="36" y="22" font-family="Inter, sans-serif" font-size="15" font-weight="700" fill="#DC2626" text-anchor="middle">{{noisiest_1_avg}}<tspan font-size="9">dB</tspan></text>
              </g>

              <!-- Peak -->
              <g transform="translate(93, 0)">
                <rect x="-6" y="-10" width="75" height="45" rx="5" fill="#FFFFFF" stroke="#FED7AA" stroke-width="1"/>
                <text x="31" y="4" font-family="Inter, sans-serif" font-size="8" font-weight="600" fill="#6B7280" letter-spacing="0.5" text-anchor="middle">PEAK</text>
                <text x="31" y="22" font-family="Inter, sans-serif" font-size="13" font-weight="600" fill="#111827" text-anchor="middle">{{noisiest_1_peak}}<tspan font-size="9">dB</tspan></text>
              </g>

              <!-- Min -->
              <g transform="translate(176, 0)">
                <rect x="-6" y="-10" width="75" height="45" rx="5" fill="#FFFFFF" stroke="#FED7AA" stroke-width="1"/>
                <text x="31" y="4" font-family="Inter, sans-serif" font-size="8" font-weight="600" fill="#6B7280" letter-spacing="0.5" text-anchor="middle">MIN</text>
                <text x="31" y="22" font-family="Inter, sans-serif" font-size="13" font-weight="600" fill="#111827" text-anchor="middle">{{noisiest_1_min}}<tspan font-size="9">dB</tspan></text>
              </g>

              <!-- Deviation -->
              <g transform="translate(259, 0)">
                <rect x="-6" y="-10" width="48" height="45" rx="5" fill="#FEE2E2" stroke="#FECACA" stroke-width="1"/>
                <text x="18" y="4" font-family="Inter, sans-serif" font-size="7" font-weight="600" fill="#6B7280" text-anchor="middle">DEV</text>
                <text x="18" y="22" font-family="Inter, sans-serif" font-size="11" font-weight="700" fill="#DC2626" text-anchor="middle">+{{noisiest_1_deviation}}</text>
              </g>
            </g>
          </g>
        </g>

        <!-- DIVIDER LINE -->
        <line x1="45" y1="910" x2="372" y2="910" stroke="#E5E7EB" stroke-width="1"/>

        <!-- QUIETEST SECTION (Bottom Half) -->
        <g id="quietest-section">
          <!-- Header with Background -->
          <rect x="30" y="915" width="357" height="35" rx="8" fill="#ECFDF5"/>
          <g transform="translate(45, 935)">
            <text font-family="Inter, sans-serif" font-size="11" font-weight="700" fill="#065F46">
              <tspan fill="#10B981">&#128264; </tspan>
              <tspan>QUIETEST LOCATION</tspan>
            </text>
          </g>

          <!-- Quietest Content -->
          <g transform="translate(45, 960)">
            <!-- Sensor Name -->
            <text font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#6B7280" letter-spacing="0.5">SENSOR: <tspan font-size="12" font-weight="700" fill="#1F2937">{{quietest_1_name}}</tspan></text>

            <!-- Metrics Row -->
            <g transform="translate(0, 25)">
              <!-- Average -->
              <g>
                <rect x="-6" y="-10" width="85" height="45" rx="5" fill="#ECFDF5" stroke="#A7F3D0" stroke-width="1"/>
                <text x="36" y="4" font-family="Inter, sans-serif" font-size="8" font-weight="700" fill="#065F46" letter-spacing="0.5" text-anchor="middle">AVG</text>
                <text x="36" y="22" font-family="Inter, sans-serif" font-size="15" font-weight="700" fill="#10B981" text-anchor="middle">{{quietest_1_avg}}<tspan font-size="9">dB</tspan></text>
              </g>

              <!-- Peak -->
              <g transform="translate(93, 0)">
                <rect x="-6" y="-10" width="75" height="45" rx="5" fill="#FFFFFF" stroke="#A7F3D0" stroke-width="1"/>
                <text x="31" y="4" font-family="Inter, sans-serif" font-size="8" font-weight="600" fill="#6B7280" letter-spacing="0.5" text-anchor="middle">PEAK</text>
                <text x="31" y="22" font-family="Inter, sans-serif" font-size="13" font-weight="600" fill="#111827" text-anchor="middle">{{quietest_1_peak}}<tspan font-size="9">dB</tspan></text>
              </g>

              <!-- Min -->
              <g transform="translate(176, 0)">
                <rect x="-6" y="-10" width="75" height="45" rx="5" fill="#FFFFFF" stroke="#A7F3D0" stroke-width="1"/>
                <text x="31" y="4" font-family="Inter, sans-serif" font-size="8" font-weight="600" fill="#6B7280" letter-spacing="0.5" text-anchor="middle">MIN</text>
                <text x="31" y="22" font-family="Inter, sans-serif" font-size="13" font-weight="600" fill="#111827" text-anchor="middle">{{quietest_1_min}}<tspan font-size="9">dB</tspan></text>
              </g>

              <!-- Deviation -->
              <g transform="translate(259, 0)">
                <rect x="-6" y="-10" width="48" height="45" rx="5" fill="#D1FAE5" stroke="#A7F3D0" stroke-width="1"/>
                <text x="18" y="4" font-family="Inter, sans-serif" font-size="7" font-weight="600" fill="#6B7280" text-anchor="middle">DEV</text>
                <text x="18" y="22" font-family="Inter, sans-serif" font-size="11" font-weight="700" fill="#10B981" text-anchor="middle">{{quietest_1_deviation}}</text>
              </g>
            </g>
          </g>
        </g>
      </g>
      <!-- End LEFT CARD -->

      <!-- RIGHT CARD: NOISE TRENDS CHART -->
      <g id="noise-trends-card">
        <rect x="407" y="785" width="357" height="240" class="card-elevated" rx="8"/>

        <!-- Chart Title -->
        <g transform="translate(417, 805)">
          <text font-family="Inter, sans-serif" font-size="11" font-weight="700" fill="#1F2937">
            {{label_noise_trends}}
          </text>
        </g>

        <!-- Chart Legend -->
        <g transform="translate(417, 820)" font-family="Inter, sans-serif">
          <!-- Max Noise Line -->
          <g transform="translate(0, 0)">
            <line x1="0" y1="5" x2="15" y2="5" stroke="#EF4444" stroke-width="2"/>
            <text x="20" y="8" font-size="8" font-weight="600" fill="#6B7280">{{label_noise_trend_max}}</text>
          </g>

          <!-- Average Noise Line -->
          <g transform="translate(55, 0)">
            <line x1="0" y1="5" x2="15" y2="5" stroke="#F59E0B" stroke-width="2"/>
            <text x="20" y="8" font-size="8" font-weight="600" fill="#6B7280">{{label_noise_trend_avg}}</text>
          </g>

          <!-- Min Noise Line -->
          <g transform="translate(130, 0)">
            <line x1="0" y1="5" x2="15" y2="5" stroke="#10B981" stroke-width="2"/>
            <text x="20" y="8" font-size="8" font-weight="600" fill="#6B7280">{{label_noise_trend_min}}</text>
          </g>

          <!-- Comfort Zone -->
          <g transform="translate(185, 0)">
            <rect x="0" y="2" width="15" height="6" fill="#10B981" opacity="0.3"/>
            <text x="20" y="8" font-size="8" font-weight="600" fill="#6B7280">{{label_noise_trend_comfort}}</text>
          </g>

          <!-- Overall Average Line -->
          <g transform="translate(260, 0)">
            <line x1="0" y1="5" x2="15" y2="5" stroke="#9CA3AF" stroke-width="2" stroke-dasharray="3,3"/>
            <text x="20" y="8" font-size="8" font-weight="600" fill="#6B7280">{{label_noise_trend_overall_avg}}</text>
          </g>
        </g>

        <!-- Chart Container -->
        <foreignObject x="417" y="840" width="337" height="170">
          <div xmlns="http://www.w3.org/1999/xhtml" id="trends-chart-container" style="width:337px; height:170px; position:relative;">
            <canvas id="noise-trends-chart" width="337" height="170" style="display:block; width:100%; height:100%;"></canvas>
          </div>
        </foreignObject>
      </g>
      <!-- End RIGHT CARD -->

    </g>
  </g>

  <!-- ========================================= -->
  <!-- FOOTER SECTION -->
  <!-- ========================================= -->
  <g id="footer">
    <line x1="30" y1="1050" x2="764" y2="1050" stroke="{{footer_bg}}" stroke-width="2"/>
    <rect y="1058" width="794" height="65" fill="{{footer_bg}}"/>
    <text x="40" y="1090" fill="{{footer_text_color}}" font-size="12" font-family="Inter, Arial, sans-serif">
      {{footer_text}}
    </text>
    <text x="754" y="1090" fill="{{footer_date_color}}" font-size="12" font-family="Inter, Arial, sans-serif" text-anchor="end">
      {{generation_date}}
    </text>
  </g>

</svg>
