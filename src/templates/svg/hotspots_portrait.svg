<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" width='210mm' height='297mm' viewBox="0 0 794 1123">
  <!-- A4 Portrait: 210mm x 297mm -->
  <!-- Template: Hotspots and Cold Zones Report (Vertical) - IMPROVED VERSION -->

  <!-- ========================================= -->
  <!-- DEFINITIONS (Styles, Gradients, Filters) -->
  <!-- ========================================= -->
  <defs>
    <style type="text/css">
      @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap');

      .metric-title { font-family: 'Inter', sans-serif; font-size: 10px; font-weight: 600; fill: {{metric_title_color}}; text-transform: uppercase; letter-spacing: 0.05em; }
      .metric-value { font-family: 'Inter', sans-serif; font-size: 22px; font-weight: 700; fill: {{metric_value_color}}; }
      .metric-unit { font-family: 'Inter', sans-serif; font-size: 11px; fill: {{metric_title_color}}; }
      .section-header { font-family: 'Inter', sans-serif; font-size: 14px; font-weight: 600; fill: {{section_header_color}}; }
      .table-header { font-family: 'Inter', sans-serif; font-size: 10px; font-weight: 600; fill: {{table_header_color}}; text-transform: uppercase; }
      .table-text { font-family: 'Inter', sans-serif; font-size: 11px; fill: {{table_text_color}}; }
      .table-small { font-family: 'Inter', sans-serif; font-size: 9px; fill: {{table_header_color}}; }

      .card { fill: #ffffff; stroke: {{card_border_color}}; stroke-width: 1; }
      .card-elevated { fill: #ffffff; stroke: {{card_border_color}}; stroke-width: 1; filter: url(#dropShadow); }
      .card-temperature { fill: {{accent_secondary}}; stroke: {{card_border_color}}; stroke-width: 1.5; }
      .card-hotspot { fill: {{accent_secondary}}; stroke: {{card_border_color}}; stroke-width: 1.5; }
      .card-coldzone { fill: {{accent_secondary}}; stroke: {{card_border_color}}; stroke-width: 1.5; }
      .divider { stroke: {{card_border_color}}; stroke-width: 0.8; }
    </style>

    <!-- Drop shadow for card elevation -->
    <filter id="dropShadow" x="-10%" y="-10%" width="120%" height="140%">
      <feDropShadow dx="0" dy="2" stdDeviation="4" flood-color="#000000" flood-opacity="0.12"/>
    </filter>

    <!-- Glass overlay effect for progress bar -->
    <linearGradient id="glassOverlay" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#FFFFFF" stop-opacity="0.3"/>
      <stop offset="45%" stop-color="#FFFFFF" stop-opacity="0.08"/>
      <stop offset="55%" stop-color="#000000" stop-opacity="0.03"/>
      <stop offset="100%" stop-color="#000000" stop-opacity="0.12"/>
    </linearGradient>

    <!-- Cold zone gradient (Blue) -->
    <linearGradient id="coldGradient" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#60A5FA"/>
      <stop offset="50%" stop-color="#3B82F6"/>
      <stop offset="100%" stop-color="#2563EB"/>
    </linearGradient>

    <!-- Comfort zone gradient (Green) -->
    <linearGradient id="comfortGradient" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#34D399"/>
      <stop offset="50%" stop-color="#10B981"/>
      <stop offset="100%" stop-color="#059669"/>
    </linearGradient>

    <!-- Hot zone gradient (Red) -->
    <linearGradient id="hotGradient" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#F87171"/>
      <stop offset="50%" stop-color="#EF4444"/>
      <stop offset="100%" stop-color="#DC2626"/>
    </linearGradient>

    <!-- Background track gradient -->
    <linearGradient id="trackGradient" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#F3F4F6"/>
      <stop offset="50%" stop-color="#E5E7EB"/>
      <stop offset="100%" stop-color="#D1D5DB"/>
    </linearGradient>

    <!-- Clip path for rounded progress bar ends -->
    <clipPath id="progressBarClip">
      <rect x="50" y="262" width="420" height="36" rx="18" ry="18"/>
    </clipPath>

  </defs>

  <!-- Background -->
  <rect width="794" height="1123" fill="#FFFFFF"/>

  <!-- ========================================= -->
  <!-- HEADER SECTION -->
  <!-- ========================================= -->
  <g id="header">
    <rect width="794" height="140" fill="{{header_bg}}"/>
    <image href="{{logo_url}}" x="35" y="15" width="105" height="105" preserveAspectRatio="xMidYMid meet"/>
    <text x="160" y="65" fill="{{header_text_color}}" font-size="24" font-family="Inter, Arial, sans-serif" font-weight="700">
      {{header_title}}
    </text>
    <text x="160" y="95" fill="{{header_subtitle_color}}" font-size="16" font-family="Inter, Arial, sans-serif" font-weight="400">
      {{header_subtitle}}
    </text>
  </g>

  <!-- ========================================= -->
  <!-- REPORT INFO BAR -->
  <!-- ========================================= -->
  <g id="report-info">
    <rect x="30" y="160" width="734" height="48" class="card-elevated" rx="6" ry="6"/>

    <!-- Building Name -->
    <g transform="translate(50, 160)">
      <text y="18" font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#6B7280" letter-spacing="0.5">NOMBRE DEL EDIFICIO</text>
      <text y="36" font-family="Inter, sans-serif" font-size="13" font-weight="600" fill="#111827">{{building_name}}</text>
    </g>

    <!-- Date Range -->
    <g transform="translate(280, 160)">
      <text y="18" font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#6B7280" letter-spacing="0.5">PERIODO DEL INFORME</text>
      <text y="36" font-family="Inter, sans-serif" font-size="13" font-weight="600" fill="#111827">{{date_range}}</text>
    </g>

    <!-- Total Sensors -->
    <g transform="translate(500, 160)">
      <text y="18" font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#6B7280" letter-spacing="0.5">TOTAL SENSORES</text>
      <text y="38" font-family="Inter, sans-serif" font-size="18" font-weight="700" fill="#111827">{{total_sensors}}</text>
    </g>

    <!-- Active Sensors -->
    <g transform="translate(640, 160)">
      <text y="18" font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#6B7280" letter-spacing="0.5">ACTIVOS</text>
      <text y="38" font-family="Inter, sans-serif" font-size="18" font-weight="700" fill="{{accent_primary}}">{{active_sensors}}</text>
    </g>
  </g>

  <!-- ========================================= -->
  <!-- TEMPERATURE DISTRIBUTION INDICATOR -->
  <!-- ========================================= -->
  <g id="temperature-comfort-indicator">
    <!-- Card Container -->
    <rect x="30" y="220" width="734" height="125" class="card-elevated" rx="8" ry="8"/>

    <!-- LEFT COLUMN: Temperature Distribution -->
    <g id="temperature-distribution-column">
      <!-- Section Header -->
      <g transform="translate(50, 248)">
        <text font-family="Inter, sans-serif" font-size="14" font-weight="700" fill="#1F2937">
          <tspan fill="#EF4444">🌡️</tspan>
          <tspan dx="6">Distribución de Temperatura</tspan>
        </text>
      </g>

      <!-- Progress Bar Container -->
      <g id="progress-bar-container">

        <!-- Background Track -->
        <rect
          x="50" y="262"
          width="420" height="36"
          fill="url(#trackGradient)"
          rx="18" ry="18"
          opacity="0.7"
        />

      <!-- Segmented Progress Bar -->
      <g clip-path="url(#progressBarClip)">

        <!-- Cold Zone Segment (Blue) -->
        <rect
          id="cold-segment"
          x="50" y="262"
          width="{{cold_bar_width_portrait}}" height="36"
          fill="url(#coldGradient)"
        >
          <animate
            attributeName="width"
            from="0" to="{{cold_bar_width_portrait}}"
            dur="0.7s"
            fill="freeze"
            calcMode="spline"
            keySplines="0.25 0.1 0.25 1"
          />
        </rect>

        <!-- Comfort Zone Segment (Green) -->
        <rect
          id="comfort-segment"
          x="{{comfort_bar_start_portrait}}" y="262"
          width="{{comfort_bar_width_portrait}}" height="36"
          fill="url(#comfortGradient)"
        >
          <animate
            attributeName="width"
            from="0" to="{{comfort_bar_width_portrait}}"
            dur="0.7s"
            begin="0.15s"
            fill="freeze"
            calcMode="spline"
            keySplines="0.25 0.1 0.25 1"
          />
        </rect>

        <!-- Hot Zone Segment (Red) -->
        <rect
          id="hot-segment"
          x="{{hot_bar_start_portrait}}" y="262"
          width="{{hot_bar_width_portrait}}" height="36"
          fill="url(#hotGradient)"
        >
          <animate
            attributeName="width"
            from="0" to="{{hot_bar_width_portrait}}"
            dur="0.7s"
            begin="0.3s"
            fill="freeze"
            calcMode="spline"
            keySplines="0.25 0.1 0.25 1"
          />
        </rect>

        <!-- Glass Overlay Effect -->
        <rect x="50" y="262" width="420" height="36" fill="url(#glassOverlay)" pointer-events="none"/>

      </g>

      <!-- Segment Dividers -->
      <g id="segment-dividers" opacity="0.4">
        <line x1="{{comfort_bar_start_portrait}}" y1="264" x2="{{comfort_bar_start_portrait}}" y2="296" stroke="#FFFFFF" stroke-width="1.5"/>
        <line x1="{{hot_bar_start_portrait}}" y1="264" x2="{{hot_bar_start_portrait}}" y2="296" stroke="#FFFFFF" stroke-width="1.5"/>
      </g>

    </g>

    <!-- Percentage Labels with Background Pills -->
    <g id="percentage-labels" font-family="Inter, sans-serif">

      <!-- Cold Percentage -->
      <g transform="translate({{cold_label_x_portrait}}, 280)" opacity="0">
        <rect x="-24" y="-13" width="48" height="22" rx="11" fill="rgba(0,0,0,0.25)"/>
        <text
          text-anchor="middle"
          font-size="13"
          font-weight="700"
          fill="#FFFFFF"
          dominant-baseline="middle"
          y="1"
        >{{cold_percentage}}%</text>
        <animate attributeName="opacity" from="0" to="1" dur="0.4s" begin="0.7s" fill="freeze"/>
      </g>

      <!-- Comfort Percentage (Emphasized) -->
      <g transform="translate({{comfort_label_x_portrait}}, 280)" opacity="0">
        <rect x="-28" y="-14" width="56" height="24" rx="12" fill="rgba(0,0,0,0.3)"/>
        <text
          text-anchor="middle"
          font-size="15"
          font-weight="800"
          fill="#FFFFFF"
          dominant-baseline="middle"
          y="1"
        >{{comfort_percentage}}%</text>
        <animate attributeName="opacity" from="0" to="1" dur="0.4s" begin="0.85s" fill="freeze"/>
      </g>

      <!-- Hot Percentage -->
      <g transform="translate({{hot_label_x_portrait}}, 280)" opacity="0">
        <rect x="-24" y="-13" width="48" height="22" rx="11" fill="rgba(0,0,0,0.25)"/>
        <text
          text-anchor="middle"
          font-size="13"
          font-weight="700"
          fill="#FFFFFF"
          dominant-baseline="middle"
          y="1"
        >{{hot_percentage}}%</text>
        <animate attributeName="opacity" from="0" to="1" dur="0.4s" begin="1s" fill="freeze"/>
      </g>

    </g>

      <!-- Legend with Badge Indicators -->
      <g id="temperature-legend" transform="translate(50, 318)" font-family="Inter, sans-serif">

        <!-- Cold Zone -->
        <g transform="translate(0, 0)">
          <rect x="-4" y="-11" width="100" height="22" rx="11" fill="#EFF6FF" stroke="#BFDBFE" stroke-width="0.5"/>
          <circle cx="10" cy="0" r="6" fill="url(#coldGradient)"/>
          <text x="22" y="4" font-size="9" font-weight="600" fill="#1E40AF">Frío (&lt;20°C)</text>
        </g>

        <!-- Comfort Zone -->
        <g transform="translate(110, 0)">
          <rect x="-4" y="-11" width="135" height="22" rx="11" fill="#ECFDF5" stroke="#A7F3D0" stroke-width="0.5"/>
          <circle cx="10" cy="0" r="6" fill="url(#comfortGradient)"/>
          <text x="22" y="4" font-size="9" font-weight="600" fill="#065F46">Confort (20-26°C)</text>
        </g>

        <!-- Hot Zone -->
        <g transform="translate(255, 0)">
          <rect x="-4" y="-11" width="105" height="22" rx="11" fill="#FEF2F2" stroke="#FECACA" stroke-width="0.5"/>
          <circle cx="10" cy="0" r="6" fill="url(#hotGradient)"/>
          <text x="22" y="4" font-size="9" font-weight="600" fill="#991B1B">Calor (&gt;26°C)</text>
        </g>

      </g>
    </g>

    <!-- RIGHT COLUMN: Comfort Zone Indicator -->
    <g id="comfort-zone-indicator">
      <!-- Section Header -->
      <g transform="translate(510, 248)">
        <text font-family="Inter, sans-serif" font-size="14" font-weight="700" fill="#1F2937">
          <tspan fill="#10B981">✓</tspan>
          <tspan dx="6">Zona de Confort</tspan>
        </text>
      </g>

      <!-- Circular Progress Indicator -->
      <g transform="translate(620, 290)">
        <!-- Background Circle -->
        <circle cx="0" cy="0" r="40" fill="none" stroke="#E5E7EB" stroke-width="8"/>

        <!-- Progress Circle (Comfort Percentage) -->
        <circle
          cx="0" cy="0" r="40"
          fill="none"
          stroke="url(#comfortGradient)"
          stroke-width="8"
          stroke-dasharray="251.2"
          stroke-dashoffset="{{comfort_circle_offset}}"
          stroke-linecap="round"
          transform="rotate(-90)"
        >
          <animate
            attributeName="stroke-dashoffset"
            from="251.2"
            to="{{comfort_circle_offset}}"
            dur="1s"
            fill="freeze"
            calcMode="spline"
            keySplines="0.25 0.1 0.25 1"
          />
        </circle>

        <!-- Percentage Text -->
        <text
          x="0" y="8"
          font-family="Inter, sans-serif"
          font-size="24"
          font-weight="800"
          fill="#10B981"
          text-anchor="middle"
        >{{comfort_percentage}}%</text>
      </g>

      <!-- Label -->
      <g transform="translate(510, 318)">
        <rect x="0" y="-11" width="200" height="22" rx="11" fill="#ECFDF5" stroke="#A7F3D0" stroke-width="0.5"/>
        <text x="100" y="4" font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#065F46" text-anchor="middle">% del tiempo en zona óptima</text>
      </g>
    </g>

  </g>

  <!-- ========================================= -->
  <!-- TEMPERATURE ANALYSIS CHART SECTION -->
  <!-- ========================================= -->
  <g id="chart-section">
    <text x="30" y="375" class="section-header">Hotspots y Zonas Frías</text>

    <rect x="30" y="385" width="734" height="280" class="card-elevated" rx="8"/>

    <!-- LEFT COLUMN: Thermometer Graphic -->
    <g id="thermometer-section" transform="translate(50, 400)">
      <image href="{{thermometer_url}}" x="30" y="10" width="120" height="240" preserveAspectRatio="xMidYMid meet"/>

      <!-- Max Temperature -->
      <g transform="translate(160, 45)">
        <rect x="-8" y="-18" width="75" height="45" rx="6" fill="#FEF2F2" stroke="#FECACA" stroke-width="0.5"/>
        <text x="0" y="0" font-family="Inter, sans-serif" font-size="9" font-weight="700" fill="#DC2626" letter-spacing="0.5">MAX</text>
        <text x="0" y="18" font-family="Inter, sans-serif" font-size="18" font-weight="700" fill="#DC2626">{{overall_max_temp}}°C</text>
      </g>

      <!-- Average Temperature -->
      <g transform="translate(160, 115)">
        <rect x="-8" y="-18" width="75" height="45" rx="6" fill="#FFFBEB" stroke="#FDE68A" stroke-width="0.5"/>
        <text x="0" y="0" font-family="Inter, sans-serif" font-size="9" font-weight="700" fill="#D97706" letter-spacing="0.5">AVG</text>
        <text x="0" y="18" font-family="Inter, sans-serif" font-size="16" font-weight="700" fill="#D97706">{{overall_avg_temp}}°C</text>
      </g>

      <!-- Min Temperature -->
      <g transform="translate(160, 185)">
        <rect x="-8" y="-18" width="75" height="45" rx="6" fill="#EFF6FF" stroke="#BFDBFE" stroke-width="0.5"/>
        <text x="0" y="0" font-family="Inter, sans-serif" font-size="9" font-weight="700" fill="#2563EB" letter-spacing="0.5">MIN</text>
        <text x="0" y="18" font-family="Inter, sans-serif" font-size="18" font-weight="700" fill="#2563EB">{{overall_min_temp}}°C</text>
      </g>
    </g>

    <!-- RIGHT COLUMN: Chart -->
    <g transform="translate(334, 395)">
      <!-- Chart Legend -->
      <g font-family="Inter, sans-serif">
        <g transform="translate(0, 0)">
          <rect width="8" height="8" rx="2" fill="#EF4444"/>
          <text x="14" y="8" font-size="9" font-weight="600" fill="#6B7280">Hotspots</text>
        </g>
        <g transform="translate(90, 0)">
          <rect width="8" height="8" rx="2" fill="#3B82F6"/>
          <text x="14" y="8" font-size="9" font-weight="600" fill="#6B7280">Cold Zones</text>
        </g>
        <g transform="translate(190, 0)">
          <line x1="0" y1="4" x2="12" y2="4" stroke="#9CA3AF" stroke-width="2" stroke-dasharray="4,2"/>
          <text x="18" y="8" font-size="9" font-weight="600" fill="#6B7280">Promedio</text>
        </g>
      </g>

      <!-- Chart Placeholder -->
      <foreignObject x="0" y="20" width="420" height="235">
        <div xmlns="http://www.w3.org/1999/xhtml" id="chart-container" style="width:100%; height:100%;">
          <canvas id="temperature-comparison-chart"></canvas>
        </div>
      </foreignObject>
    </g>
  </g>

  <!-- ========================================= -->
  <!-- SENSOR DETAILS SECTION -->
  <!-- ========================================= -->
  <g id="hotspots-coldzones">
    <text x="30" y="695" class="section-header">Detalles de Temperatura por Sensor</text>

    <!-- SINGLE CARD: HOTSPOT & COLD ZONE -->
    <g id="temperature-extremes-card">
      <rect x="30" y="705" width="357" height="240" class="card-elevated" rx="8"/>

      <!-- HOTSPOT SECTION (Top Half) -->
      <g id="hotspot-section">
        <!-- Hotspot Header with Background -->
        <rect x="30" y="705" width="357" height="35" rx="8" fill="#FEF2F2"/>
        <g transform="translate(45, 725)">
          <text font-family="Inter, sans-serif" font-size="11" font-weight="700" fill="#DC2626">
            <tspan fill="#EF4444">🔥 </tspan>
            <tspan>HOTSPOT</tspan>
          </text>
        </g>

        <!-- Hotspot Content -->
        <g transform="translate(45, 750)">
          <!-- Sensor Name -->
          <text font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#6B7280" letter-spacing="0.5">SENSOR: <tspan font-size="12" font-weight="700" fill="#1F2937">{{hotspot_1_name}}</tspan></text>

          <!-- Metrics Row -->
          <g transform="translate(0, 25)">
            <!-- Average -->
            <g>
              <rect x="-6" y="-10" width="85" height="45" rx="5" fill="#FEF2F2" stroke="#FECACA" stroke-width="1"/>
              <text x="36" y="4" font-family="Inter, sans-serif" font-size="8" font-weight="700" fill="#DC2626" letter-spacing="0.5" text-anchor="middle">PROM</text>
              <text x="36" y="22" font-family="Inter, sans-serif" font-size="15" font-weight="700" fill="#DC2626" text-anchor="middle">{{hotspot_1_avg}}<tspan font-size="9">°C</tspan></text>
            </g>

            <!-- Min -->
            <g transform="translate(93, 0)">
              <rect x="-6" y="-10" width="75" height="45" rx="5" fill="#FFFFFF" stroke="#FED7AA" stroke-width="1"/>
              <text x="31" y="4" font-family="Inter, sans-serif" font-size="8" font-weight="600" fill="#6B7280" letter-spacing="0.5" text-anchor="middle">MIN</text>
              <text x="31" y="22" font-family="Inter, sans-serif" font-size="13" font-weight="600" fill="#111827" text-anchor="middle">{{hotspot_1_min}}<tspan font-size="9">°C</tspan></text>
            </g>

            <!-- Max -->
            <g transform="translate(176, 0)">
              <rect x="-6" y="-10" width="75" height="45" rx="5" fill="#FFFFFF" stroke="#FED7AA" stroke-width="1"/>
              <text x="31" y="4" font-family="Inter, sans-serif" font-size="8" font-weight="600" fill="#6B7280" letter-spacing="0.5" text-anchor="middle">MAX</text>
              <text x="31" y="22" font-family="Inter, sans-serif" font-size="13" font-weight="600" fill="#111827" text-anchor="middle">{{hotspot_1_max}}<tspan font-size="9">°C</tspan></text>
            </g>

            <!-- Deviation -->
            <g transform="translate(259, 0)">
              <rect x="-6" y="-10" width="48" height="45" rx="5" fill="#FEE2E2" stroke="#FECACA" stroke-width="1"/>
              <text x="18" y="4" font-family="Inter, sans-serif" font-size="7" font-weight="600" fill="#6B7280" text-anchor="middle">DESV</text>
              <text x="18" y="22" font-family="Inter, sans-serif" font-size="11" font-weight="700" fill="#DC2626" text-anchor="middle">+{{hotspot_1_deviation}}</text>
            </g>
          </g>
        </g>
      </g>

      <!-- DIVIDER LINE -->
      <line x1="45" y1="830" x2="372" y2="830" stroke="#E5E7EB" stroke-width="1"/>

      <!-- COLD ZONE SECTION (Bottom Half) -->
      <g id="coldzone-section">
        <!-- Cold Zone Header with Background -->
        <rect x="30" y="835" width="357" height="35" rx="8" fill="#EFF6FF"/>
        <g transform="translate(45, 855)">
          <text font-family="Inter, sans-serif" font-size="11" font-weight="700" fill="#1D4ED8">
            <tspan fill="#3B82F6">❄️ </tspan>
            <tspan>COLD ZONE</tspan>
          </text>
        </g>

        <!-- Cold Zone Content -->
        <g transform="translate(45, 880)">
          <!-- Sensor Name -->
          <text font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#6B7280" letter-spacing="0.5">SENSOR: <tspan font-size="12" font-weight="700" fill="#1F2937">{{coldzone_1_name}}</tspan></text>

          <!-- Metrics Row -->
          <g transform="translate(0, 25)">
            <!-- Average -->
            <g>
              <rect x="-6" y="-10" width="85" height="45" rx="5" fill="#EFF6FF" stroke="#BFDBFE" stroke-width="1"/>
              <text x="36" y="4" font-family="Inter, sans-serif" font-size="8" font-weight="700" fill="#1D4ED8" letter-spacing="0.5" text-anchor="middle">PROM</text>
              <text x="36" y="22" font-family="Inter, sans-serif" font-size="15" font-weight="700" fill="#1D4ED8" text-anchor="middle">{{coldzone_1_avg}}<tspan font-size="9">°C</tspan></text>
            </g>

            <!-- Min -->
            <g transform="translate(93, 0)">
              <rect x="-6" y="-10" width="75" height="45" rx="5" fill="#FFFFFF" stroke="#BAE6FD" stroke-width="1"/>
              <text x="31" y="4" font-family="Inter, sans-serif" font-size="8" font-weight="600" fill="#6B7280" letter-spacing="0.5" text-anchor="middle">MIN</text>
              <text x="31" y="22" font-family="Inter, sans-serif" font-size="13" font-weight="600" fill="#111827" text-anchor="middle">{{coldzone_1_min}}<tspan font-size="9">°C</tspan></text>
            </g>

            <!-- Max -->
            <g transform="translate(176, 0)">
              <rect x="-6" y="-10" width="75" height="45" rx="5" fill="#FFFFFF" stroke="#BAE6FD" stroke-width="1"/>
              <text x="31" y="4" font-family="Inter, sans-serif" font-size="8" font-weight="600" fill="#6B7280" letter-spacing="0.5" text-anchor="middle">MAX</text>
              <text x="31" y="22" font-family="Inter, sans-serif" font-size="13" font-weight="600" fill="#111827" text-anchor="middle">{{coldzone_1_max}}<tspan font-size="9">°C</tspan></text>
            </g>

            <!-- Deviation -->
            <g transform="translate(259, 0)">
              <rect x="-6" y="-10" width="48" height="45" rx="5" fill="#DBEAFE" stroke="#BFDBFE" stroke-width="1"/>
              <text x="18" y="4" font-family="Inter, sans-serif" font-size="7" font-weight="600" fill="#6B7280" text-anchor="middle">DESV</text>
              <text x="18" y="22" font-family="Inter, sans-serif" font-size="11" font-weight="700" fill="#1D4ED8" text-anchor="middle">{{coldzone_1_deviation}}</text>
            </g>
          </g>
        </g>
      </g>
    </g>
  </g>

  <!-- ========================================= -->
  <!-- FOOTER SECTION -->
  <!-- ========================================= -->
  <g id="footer">
    <line x1="30" y1="1050" x2="764" y2="1050" stroke="{{footer_bg}}" stroke-width="2"/>
    <rect y="1058" width="794" height="65" fill="{{footer_bg}}"/>
    <text x="40" y="1090" fill="{{footer_text_color}}" font-size="12" font-family="Inter, Arial, sans-serif">
      {{footer_text}}
    </text>
    <text x="754" y="1090" fill="{{footer_date_color}}" font-size="12" font-family="Inter, Arial, sans-serif" text-anchor="end">
      {{generation_date}}
    </text>
  </g>

</svg>
