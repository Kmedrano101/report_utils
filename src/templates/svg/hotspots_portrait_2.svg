<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" width='210mm' height='297mm' viewBox="0 0 794 1123">
  <!-- A4 Portrait: 210mm x 297mm -->
  <!-- Template: Hotspots and Cold Zones Report (Vertical) - IMPROVED VERSION -->
  <!-- Description: Vertical A4 report template for visualizing temperature extremes (hotspots/cold zones) with comfort indicators, distribution charts, and sensor-specific metrics. -->

  <!-- ========================================= -->
  <!-- DEFINITIONS (Styles, Gradients, Filters) -->
  <!-- ========================================= -->
  <defs>
    <style type="text/css">
      @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap');

      .metric-title { font-family: 'Inter', sans-serif; font-size: 10px; font-weight: 600; fill: {{metric_title_color}}; text-transform: uppercase; letter-spacing: 0.05em; }
      .metric-value { font-family: 'Inter', sans-serif; font-size: 22px; font-weight: 700; fill: {{metric_value_color}}; }
      .metric-unit { font-family: 'Inter', sans-serif; font-size: 11px; fill: {{metric_title_color}}; }
      .section-header { font-family: 'Inter', sans-serif; font-size: 14px; font-weight: 600; fill: {{section_header_color}}; }
      .table-header { font-family: 'Inter', sans-serif; font-size: 10px; font-weight: 600; fill: {{table_header_color}}; text-transform: uppercase; }
      .table-text { font-family: 'Inter', sans-serif; font-size: 11px; fill: {{table_text_color}}; }
      .table-small { font-family: 'Inter', sans-serif; font-size: 9px; fill: {{table_header_color}}; }

      .card { fill: #ffffff; stroke: {{card_border_color}}; stroke-width: 1; }
      .card-elevated { fill: #ffffff; stroke: {{card_border_color}}; stroke-width: 1; filter: url(#dropShadow); }
      .card-temperature { fill: {{accent_secondary}}; stroke: {{card_border_color}}; stroke-width: 1.5; }
      .card-hotspot { fill: {{accent_secondary}}; stroke: {{card_border_color}}; stroke-width: 1.5; }
      .card-coldzone { fill: {{accent_secondary}}; stroke: {{card_border_color}}; stroke-width: 1.5; }
      .divider { stroke: {{card_border_color}}; stroke-width: 0.8; }
    </style>

    <!-- Drop shadow for card elevation -->
    <filter id="dropShadow" x="-10%" y="-10%" width="120%" height="140%">
      <feDropShadow dx="0" dy="2" stdDeviation="4" flood-color="#000000" flood-opacity="0.12"/>
    </filter>

    <!-- Glass overlay effect for progress bar -->
    <linearGradient id="glassOverlay" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#FFFFFF" stop-opacity="0.3"/>
      <stop offset="45%" stop-color="#FFFFFF" stop-opacity="0.08"/>
      <stop offset="55%" stop-color="#000000" stop-opacity="0.03"/>
      <stop offset="100%" stop-color="#000000" stop-opacity="0.12"/>
    </linearGradient>

    <!-- Cold zone gradient (Blue) -->
    <linearGradient id="coldGradient" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#60A5FA"/>
      <stop offset="50%" stop-color="#3B82F6"/>
      <stop offset="100%" stop-color="#2563EB"/>
    </linearGradient>

    <!-- Comfort zone gradient (Green) -->
    <linearGradient id="comfortGradient" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#34D399"/>
      <stop offset="50%" stop-color="#10B981"/>
      <stop offset="100%" stop-color="#059669"/>
    </linearGradient>

    <!-- Hot zone gradient (Red) -->
    <linearGradient id="hotGradient" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#F87171"/>
      <stop offset="50%" stop-color="#EF4444"/>
      <stop offset="100%" stop-color="#DC2626"/>
    </linearGradient>

    <!-- Background track gradient -->
    <linearGradient id="trackGradient" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#F3F4F6"/>
      <stop offset="50%" stop-color="#E5E7EB"/>
      <stop offset="100%" stop-color="#D1D5DB"/>
    </linearGradient>

    <!-- Clip path for rounded progress bar ends -->
    <clipPath id="progressBarClip">
      <rect x="50" y="262" width="420" height="36" rx="18" ry="18"/>
    </clipPath>

  </defs>

  <!-- Background -->
  <rect width="794" height="1123" fill="#FFFFFF"/>

  <!-- ========================================= -->
  <!-- HEADER SECTION -->
  <!-- ========================================= -->
  <g id="header">
    <rect width="794" height="140" fill="{{header_bg}}"/>
    <image href="{{logo_url}}" x="35" y="15" width="105" height="105" preserveAspectRatio="xMidYMid meet"/>
    <text x="160" y="65" fill="{{header_text_color}}" font-size="24" font-family="Inter, Arial, sans-serif" font-weight="700">
      {{header_title}}
    </text>
    <text x="160" y="95" fill="{{header_subtitle_color}}" font-size="16" font-family="Inter, Arial, sans-serif" font-weight="400">
      {{header_subtitle}}
    </text>
  </g>

  <!-- ========================================= -->
  <!-- REPORT INFO BAR -->
  <!-- ========================================= -->
  <g id="report-info">
    <rect x="30" y="150" width="734" height="48" class="card-elevated" rx="6" ry="6"/>

    <!-- Building Name -->
    <g transform="translate(50, 150)">
      <text y="18" font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#6B7280" letter-spacing="0.5">{{label_building_name}}</text>
      <text y="36" font-family="Inter, sans-serif" font-size="13" font-weight="600" fill="#111827">{{building_name}}</text>
    </g>

    <!-- Date Range -->
    <g transform="translate(280, 150)">
      <text y="18" font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#6B7280" letter-spacing="0.5">{{label_report_period}}</text>
      <text y="36" font-family="Inter, sans-serif" font-size="13" font-weight="600" fill="#111827">{{date_range}}</text>
    </g>

    <!-- Total Sensors -->
    <g transform="translate(500, 150)">
      <text y="18" font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#6B7280" letter-spacing="0.5">{{label_total_sensors}}</text>
      <text y="38" font-family="Inter, sans-serif" font-size="18" font-weight="700" fill="#111827">{{total_sensors}}</text>
    </g>

    <!-- Active Sensors -->
    <g transform="translate(640, 150)">
      <text y="18" font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#6B7280" letter-spacing="0.5">{{label_active}}</text>
      <text y="38" font-family="Inter, sans-serif" font-size="18" font-weight="700" fill="{{accent_primary}}">{{active_sensors}}</text>
    </g>
  </g>

  <!-- ========================================= -->
  <!-- REPORT DESCRIPTION SECTION -->
  <!-- ========================================= -->
  <g id="report-description">
    <text x="30" y="220" class="section-header">{{label_report_overview}}</text>

    <rect x="30" y="230" width="734" height="60" class="card-elevated" rx="8" ry="8"/>

    <!-- Description Text -->
    <foreignObject x="50" y="242" width="694" height="42">
      <div xmlns="http://www.w3.org/1999/xhtml" style="font-family: 'Inter', sans-serif; font-size: 10px; line-height: 1.5; color: #4B5563; text-align: justify;">
        {{report_description}}
      </div>
    </foreignObject>
  </g>

  <!-- ========================================= -->
  <!-- TEMPERATURE DISTRIBUTION INDICATOR -->
  <!-- ========================================= -->
  <g id="temperature-comfort-indicator">
    <!-- Card Container -->
    <rect x="30" y="300" width="734" height="125" class="card-elevated" rx="8" ry="8"/>

    <!-- LEFT COLUMN: Key Metrics -->
    <g id="key-metrics-column">
      <!-- Section Header -->
      <g transform="translate(50, 328)">
        <text font-family="Inter, sans-serif" font-size="14" font-weight="700" fill="#1F2937">
          {{label_key_metrics}}
        </text>
      </g>

      <!-- Comfort Metric Card -->
      <g transform="translate(50, 345)">
        <rect x="0" y="0" width="145" height="68" rx="8" fill="#F0FDF4" stroke="#BBF7D0" stroke-width="1"/>
        <!-- Icon -->
        <circle cx="24" cy="34" r="16" fill="#DCFCE7"/>
        <text x="24" y="39" font-family="Inter, sans-serif" font-size="14" text-anchor="middle" fill="#16A34A">&#10003;</text>
        <!-- Content -->
        <text x="52" y="22" font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#166534" letter-spacing="0.3">{{label_comfort}}</text>
        <text x="52" y="46" font-family="Inter, sans-serif" font-size="26" font-weight="800" fill="#15803D">{{comfort_percentage}}<tspan font-size="14" font-weight="600">%</tspan></text>
        <text x="52" y="60" font-family="Inter, sans-serif" font-size="8" fill="#4ADE80">{{label_ideal_zone}}</text>
      </g>

      <!-- Critical Metric Card -->
      <g transform="translate(205, 345)">
        <rect x="0" y="0" width="145" height="68" rx="8" fill="#FEF2F2" stroke="#FECACA" stroke-width="1"/>
        <!-- Icon -->
        <circle cx="24" cy="34" r="16" fill="#FEE2E2"/>
        <text x="24" y="39" font-family="Inter, sans-serif" font-size="14" text-anchor="middle" fill="#DC2626">!</text>
        <!-- Content -->
        <text x="52" y="22" font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#991B1B" letter-spacing="0.3">{{label_critical}}</text>
        <text x="52" y="46" font-family="Inter, sans-serif" font-size="26" font-weight="800" fill="#DC2626">{{critical_percentage}}<tspan font-size="14" font-weight="600">%</tspan></text>
        <text x="52" y="60" font-family="Inter, sans-serif" font-size="8" fill="#F87171">{{label_out_of_range}}</text>
      </g>
    </g>

    <!-- RIGHT COLUMN: Temperature Distribution Donut Chart -->
    <g id="temperature-distribution-chart">
      <!-- Section Header -->
      <g transform="translate(400, 328)">
        <text font-family="Inter, sans-serif" font-size="14" font-weight="700" fill="#1F2937">
          {{label_temp_distribution}}
        </text>
      </g>

      <!-- Donut Chart -->
      <g transform="translate(540, 380)">
        <!-- Background Circle -->
        <circle cx="0" cy="0" r="31.5" fill="none" stroke="#F3F4F6" stroke-width="15"/>

        <!-- Cold Segment (Blue) - starts at top (-90deg) -->
        <circle
          cx="0" cy="0" r="31.5"
          fill="none"
          stroke="#3B82F6"
          stroke-width="15"
          stroke-dasharray="{{cold_arc_length}} 198"
          stroke-dashoffset="0"
          transform="rotate(-90)"
        >
          <animate
            attributeName="stroke-dasharray"
            from="0 198" to="{{cold_arc_length}} 198"
            dur="0.8s"
            fill="freeze"
            calcMode="spline"
            keySplines="0.4 0 0.2 1"
          />
        </circle>

        <!-- Comfort Segment (Green) - starts after cold -->
        <circle
          cx="0" cy="0" r="31.5"
          fill="none"
          stroke="#10B981"
          stroke-width="15"
          stroke-dasharray="{{comfort_arc_length}} 198"
          stroke-dashoffset="{{comfort_arc_offset}}"
          transform="rotate(-90)"
        >
          <animate
            attributeName="stroke-dasharray"
            from="0 198" to="{{comfort_arc_length}} 198"
            dur="0.8s"
            begin="0.2s"
            fill="freeze"
            calcMode="spline"
            keySplines="0.4 0 0.2 1"
          />
        </circle>

        <!-- Hot Segment (Red) - starts after comfort -->
        <circle
          cx="0" cy="0" r="31.5"
          fill="none"
          stroke="#EF4444"
          stroke-width="15"
          stroke-dasharray="{{hot_arc_length}} 198"
          stroke-dashoffset="{{hot_arc_offset}}"
          transform="rotate(-90)"
        >
          <animate
            attributeName="stroke-dasharray"
            from="0 198" to="{{hot_arc_length}} 198"
            dur="0.8s"
            begin="0.4s"
            fill="freeze"
            calcMode="spline"
            keySplines="0.4 0 0.2 1"
          />
        </circle>

        <!-- Center Text -->
        <text x="0" y="-5" font-family="Inter, sans-serif" font-size="10" font-weight="600" fill="#6B7280" text-anchor="middle">{{label_total}}</text>
        <text x="0" y="12" font-family="Inter, sans-serif" font-size="16" font-weight="700" fill="#1F2937" text-anchor="middle">100%</text>
      </g>

      <!-- Legend -->
      <g transform="translate(620, 338)" font-family="Inter, sans-serif">
        <!-- Cold -->
        <g transform="translate(0, 0)">
          <rect x="0" y="0" width="10" height="10" rx="2" fill="#3B82F6"/>
          <text x="16" y="9" font-size="9" font-weight="600" fill="#1E40AF">{{label_cold}}</text>
          <text x="50" y="9" font-size="9" font-weight="700" fill="#1F2937">{{cold_percentage}}%</text>
        </g>

        <!-- Comfort -->
        <g transform="translate(0, 20)">
          <rect x="0" y="0" width="10" height="10" rx="2" fill="#10B981"/>
          <text x="16" y="9" font-size="9" font-weight="600" fill="#065F46">{{label_comfort}}</text>
          <text x="60" y="9" font-size="9" font-weight="700" fill="#1F2937">{{comfort_percentage}}%</text>
        </g>

        <!-- Hot -->
        <g transform="translate(0, 40)">
          <rect x="0" y="0" width="10" height="10" rx="2" fill="#EF4444"/>
          <text x="16" y="9" font-size="9" font-weight="600" fill="#991B1B">{{label_heat}}</text>
          <text x="50" y="9" font-size="9" font-weight="700" fill="#1F2937">{{hot_percentage}}%</text>
        </g>
      </g>

      <!-- Temperature Ranges Label -->
      <g transform="translate(620, 400)" font-family="Inter, sans-serif">
        <text font-size="7" fill="#9CA3AF">{{label_temp_ranges}}</text>
      </g>
    </g>

  </g>

  <!-- ========================================= -->
  <!-- TEMPERATURE ANALYSIS CHART SECTION -->
  <!-- ========================================= -->
  <g id="chart-section">
    <text x="30" y="445" class="section-header">{{label_hotspots_coldzones}}</text>

    <rect x="30" y="455" width="734" height="280" class="card-elevated" rx="8"/>

    <!-- LEFT COLUMN: Thermometer Graphic -->
    <g id="thermometer-section" transform="translate(50, 470)">
      <image href="{{thermometer_url}}" x="30" y="10" width="120" height="240" preserveAspectRatio="xMidYMid meet"/>

      <!-- Max Temperature -->
      <g transform="translate(160, 45)">
        <rect x="-8" y="-18" width="75" height="45" rx="6" fill="#FEF2F2" stroke="#FECACA" stroke-width="0.5"/>
        <text x="0" y="0" font-family="Inter, sans-serif" font-size="9" font-weight="700" fill="#DC2626" letter-spacing="0.5">{{label_max}}</text>
        <text x="0" y="18" font-family="Inter, sans-serif" font-size="18" font-weight="700" fill="#DC2626">{{overall_max_temp}}°C</text>
      </g>

      <!-- Average Temperature -->
      <g transform="translate(160, 115)">
        <rect x="-8" y="-18" width="75" height="45" rx="6" fill="#FFFBEB" stroke="#FDE68A" stroke-width="0.5"/>
        <text x="0" y="0" font-family="Inter, sans-serif" font-size="9" font-weight="700" fill="#D97706" letter-spacing="0.5">{{label_avg}}</text>
        <text x="0" y="18" font-family="Inter, sans-serif" font-size="16" font-weight="700" fill="#D97706">{{overall_avg_temp}}°C</text>
      </g>

      <!-- Min Temperature -->
      <g transform="translate(160, 185)">
        <rect x="-8" y="-18" width="75" height="45" rx="6" fill="#EFF6FF" stroke="#BFDBFE" stroke-width="0.5"/>
        <text x="0" y="0" font-family="Inter, sans-serif" font-size="9" font-weight="700" fill="#2563EB" letter-spacing="0.5">{{label_min}}</text>
        <text x="0" y="18" font-family="Inter, sans-serif" font-size="18" font-weight="700" fill="#2563EB">{{overall_min_temp}}°C</text>
      </g>
    </g>

    <!-- RIGHT COLUMN: Chart -->
    <g transform="translate(334, 465)">
      <!-- Chart Legend -->
      <g font-family="Inter, sans-serif">
        <g transform="translate(0, 0)">
          <rect width="8" height="8" rx="2" fill="#EF4444"/>
          <text x="14" y="8" font-size="9" font-weight="600" fill="#6B7280">Hotspots</text>
        </g>
        <g transform="translate(90, 0)">
          <rect width="8" height="8" rx="2" fill="#3B82F6"/>
          <text x="14" y="8" font-size="9" font-weight="600" fill="#6B7280">Cold Zones</text>
        </g>
        <g transform="translate(190, 0)">
          <line x1="0" y1="4" x2="12" y2="4" stroke="#9CA3AF" stroke-width="2" stroke-dasharray="4,2"/>
          <text x="18" y="8" font-size="9" font-weight="600" fill="#6B7280">{{label_average}}</text>
        </g>
      </g>

      <!-- Chart Placeholder -->
      <foreignObject x="0" y="20" width="420" height="235">
        <div xmlns="http://www.w3.org/1999/xhtml" id="chart-container" style="width:100%; height:100%;">
          <canvas id="temperature-comparison-chart"></canvas>
        </div>
      </foreignObject>
    </g>
  </g>

  <!-- ========================================= -->
  <!-- SENSOR DETAILS SECTION -->
  <!-- ========================================= -->
  <g id="hotspots-coldzones" transform="translate(0, 20)">
    <text x="30" y="755" class="section-header">{{label_temp_details}}</text>

    <!-- SINGLE CARD: HOTSPOT & COLD ZONE -->
    <g id="temperature-extremes-card">
      <rect x="30" y="765" width="357" height="240" class="card-elevated" rx="8"/>

      <!-- HOTSPOT SECTION (Top Half) -->
      <g id="hotspot-section">
        <!-- Hotspot Header with Background -->
        <rect x="30" y="765" width="357" height="35" rx="8" fill="#FEF2F2"/>
        <g transform="translate(45, 785)">
          <text font-family="Inter, sans-serif" font-size="11" font-weight="700" fill="#DC2626">
            <tspan fill="#EF4444">🔥 </tspan>
            <tspan>{{label_hotspot}}</tspan>
          </text>
        </g>

        <!-- Hotspot Content -->
        <g transform="translate(45, 810)">
          <!-- Sensor Name -->
          <text font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#6B7280" letter-spacing="0.5">{{label_sensor}}: <tspan font-size="12" font-weight="700" fill="#1F2937">{{hotspot_1_name}}</tspan></text>

          <!-- Metrics Row -->
          <g transform="translate(0, 25)">
            <!-- Average -->
            <g>
              <rect x="-6" y="-10" width="85" height="45" rx="5" fill="#FEF2F2" stroke="#FECACA" stroke-width="1"/>
              <text x="36" y="4" font-family="Inter, sans-serif" font-size="8" font-weight="700" fill="#DC2626" letter-spacing="0.5" text-anchor="middle">{{label_prom}}</text>
              <text x="36" y="22" font-family="Inter, sans-serif" font-size="15" font-weight="700" fill="#DC2626" text-anchor="middle">{{hotspot_1_avg}}<tspan font-size="9">°C</tspan></text>
            </g>

            <!-- Min -->
            <g transform="translate(93, 0)">
              <rect x="-6" y="-10" width="75" height="45" rx="5" fill="#FFFFFF" stroke="#FED7AA" stroke-width="1"/>
              <text x="31" y="4" font-family="Inter, sans-serif" font-size="8" font-weight="600" fill="#6B7280" letter-spacing="0.5" text-anchor="middle">{{label_min}}</text>
              <text x="31" y="22" font-family="Inter, sans-serif" font-size="13" font-weight="600" fill="#111827" text-anchor="middle">{{hotspot_1_min}}<tspan font-size="9">°C</tspan></text>
            </g>

            <!-- Max -->
            <g transform="translate(176, 0)">
              <rect x="-6" y="-10" width="75" height="45" rx="5" fill="#FFFFFF" stroke="#FED7AA" stroke-width="1"/>
              <text x="31" y="4" font-family="Inter, sans-serif" font-size="8" font-weight="600" fill="#6B7280" letter-spacing="0.5" text-anchor="middle">{{label_max}}</text>
              <text x="31" y="22" font-family="Inter, sans-serif" font-size="13" font-weight="600" fill="#111827" text-anchor="middle">{{hotspot_1_max}}<tspan font-size="9">°C</tspan></text>
            </g>

            <!-- Deviation -->
            <g transform="translate(259, 0)">
              <rect x="-6" y="-10" width="48" height="45" rx="5" fill="#FEE2E2" stroke="#FECACA" stroke-width="1"/>
              <text x="18" y="4" font-family="Inter, sans-serif" font-size="7" font-weight="600" fill="#6B7280" text-anchor="middle">{{label_dev}}</text>
              <text x="18" y="22" font-family="Inter, sans-serif" font-size="11" font-weight="700" fill="#DC2626" text-anchor="middle">+{{hotspot_1_deviation}}</text>
            </g>
          </g>
        </g>
      </g>

      <!-- DIVIDER LINE -->
      <line x1="45" y1="890" x2="372" y2="890" stroke="#E5E7EB" stroke-width="1"/>

      <!-- COLD ZONE SECTION (Bottom Half) -->
      <g id="coldzone-section">
        <!-- Cold Zone Header with Background -->
        <rect x="30" y="895" width="357" height="35" rx="8" fill="#EFF6FF"/>
        <g transform="translate(45, 915)">
          <text font-family="Inter, sans-serif" font-size="11" font-weight="700" fill="#1D4ED8">
            <tspan fill="#3B82F6">❄️ </tspan>
            <tspan>{{label_cold_zone}}</tspan>
          </text>
        </g>

        <!-- Cold Zone Content -->
        <g transform="translate(45, 940)">
          <!-- Sensor Name -->
          <text font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#6B7280" letter-spacing="0.5">{{label_sensor}}: <tspan font-size="12" font-weight="700" fill="#1F2937">{{coldzone_1_name}}</tspan></text>

          <!-- Metrics Row -->
          <g transform="translate(0, 25)">
            <!-- Average -->
            <g>
              <rect x="-6" y="-10" width="85" height="45" rx="5" fill="#EFF6FF" stroke="#BFDBFE" stroke-width="1"/>
              <text x="36" y="4" font-family="Inter, sans-serif" font-size="8" font-weight="700" fill="#1D4ED8" letter-spacing="0.5" text-anchor="middle">{{label_prom}}</text>
              <text x="36" y="22" font-family="Inter, sans-serif" font-size="15" font-weight="700" fill="#1D4ED8" text-anchor="middle">{{coldzone_1_avg}}<tspan font-size="9">°C</tspan></text>
            </g>

            <!-- Min -->
            <g transform="translate(93, 0)">
              <rect x="-6" y="-10" width="75" height="45" rx="5" fill="#FFFFFF" stroke="#BAE6FD" stroke-width="1"/>
              <text x="31" y="4" font-family="Inter, sans-serif" font-size="8" font-weight="600" fill="#6B7280" letter-spacing="0.5" text-anchor="middle">{{label_min}}</text>
              <text x="31" y="22" font-family="Inter, sans-serif" font-size="13" font-weight="600" fill="#111827" text-anchor="middle">{{coldzone_1_min}}<tspan font-size="9">°C</tspan></text>
            </g>

            <!-- Max -->
            <g transform="translate(176, 0)">
              <rect x="-6" y="-10" width="75" height="45" rx="5" fill="#FFFFFF" stroke="#BAE6FD" stroke-width="1"/>
              <text x="31" y="4" font-family="Inter, sans-serif" font-size="8" font-weight="600" fill="#6B7280" letter-spacing="0.5" text-anchor="middle">{{label_max}}</text>
              <text x="31" y="22" font-family="Inter, sans-serif" font-size="13" font-weight="600" fill="#111827" text-anchor="middle">{{coldzone_1_max}}<tspan font-size="9">°C</tspan></text>
            </g>

            <!-- Deviation -->
            <g transform="translate(259, 0)">
              <rect x="-6" y="-10" width="48" height="45" rx="5" fill="#DBEAFE" stroke="#BFDBFE" stroke-width="1"/>
              <text x="18" y="4" font-family="Inter, sans-serif" font-size="7" font-weight="600" fill="#6B7280" text-anchor="middle">{{label_dev}}</text>
              <text x="18" y="22" font-family="Inter, sans-serif" font-size="11" font-weight="700" fill="#1D4ED8" text-anchor="middle">{{coldzone_1_deviation}}</text>
            </g>
          </g>
        </g>
      </g>
    </g>
  </g>

  <!-- ========================================= -->
  <!-- FOOTER SECTION -->
  <!-- ========================================= -->
  <g id="footer">
    <rect y="1050" width="794" height="73" fill="{{footer_bg}}"/>
    <text x="40" y="1090" fill="{{footer_text_color}}" font-size="12" font-family="Inter, Arial, sans-serif">
      {{footer_text}}
    </text>
    <text x="754" y="1090" fill="{{footer_date_color}}" font-size="12" font-family="Inter, Arial, sans-serif" text-anchor="end">
      {{generation_date}}
    </text>
  </g>

</svg>
