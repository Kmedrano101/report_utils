<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" width='297mm' height='210mm' viewBox="0 0 1123 794">
  <!-- A4 Landscape: 297mm x 210mm -->
  <!-- Template: Hotspots and Cold Zones Report - IMPROVED VERSION -->

  <!-- ========================================= -->
  <!-- DEFINITIONS (Styles, Gradients, Filters) -->
  <!-- ========================================= -->
  <defs>
    <style type="text/css">
      @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&amp;display=swap');

      .metric-title { font-family: 'Inter', sans-serif; font-size: 10px; font-weight: 600; fill: {{metric_title_color}}; text-transform: uppercase; letter-spacing: 0.05em; }
      .metric-value { font-family: 'Inter', sans-serif; font-size: 22px; font-weight: 700; fill: {{metric_value_color}}; }
      .metric-unit { font-family: 'Inter', sans-serif; font-size: 11px; fill: {{metric_title_color}}; }
      .section-header { font-family: 'Inter', sans-serif; font-size: 14px; font-weight: 600; fill: {{section_header_color}}; }
      .table-header { font-family: 'Inter', sans-serif; font-size: 10px; font-weight: 600; fill: {{table_header_color}}; text-transform: uppercase; }
      .table-text { font-family: 'Inter', sans-serif; font-size: 11px; fill: {{table_text_color}}; }
      .table-small { font-family: 'Inter', sans-serif; font-size: 9px; fill: {{table_header_color}}; }

      .card { fill: #ffffff; stroke: {{card_border_color}}; stroke-width: 1; }
      .card-elevated { fill: #ffffff; stroke: {{card_border_color}}; stroke-width: 1; filter: url(#dropShadow); }
      .card-temperature { fill: {{accent_secondary}}; stroke: {{card_border_color}}; stroke-width: 1.5; }
      .card-hotspot { fill: {{accent_secondary}}; stroke: {{card_border_color}}; stroke-width: 1.5; }
      .card-coldzone { fill: {{accent_secondary}}; stroke: {{card_border_color}}; stroke-width: 1.5; }
      .divider { stroke: {{card_border_color}}; stroke-width: 0.8; }
    </style>

    <!-- ========================================= -->
    <!-- FILTERS -->
    <!-- ========================================= -->

    <!-- Drop shadow for card elevation -->
    <filter id="dropShadow" x="-10%" y="-10%" width="120%" height="140%">
      <feDropShadow dx="0" dy="2" stdDeviation="4" flood-color="#000000" flood-opacity="0.12"/>
    </filter>

    <!-- ========================================= -->
    <!-- GRADIENTS -->
    <!-- ========================================= -->

    <!-- Glass overlay effect for progress bar -->
    <linearGradient id="glassOverlay" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#FFFFFF" stop-opacity="0.3"/>
      <stop offset="45%" stop-color="#FFFFFF" stop-opacity="0.08"/>
      <stop offset="55%" stop-color="#000000" stop-opacity="0.03"/>
      <stop offset="100%" stop-color="#000000" stop-opacity="0.12"/>
    </linearGradient>

    <!-- Cold zone gradient (Blue) -->
    <linearGradient id="coldGradient" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#60A5FA"/>
      <stop offset="50%" stop-color="#3B82F6"/>
      <stop offset="100%" stop-color="#2563EB"/>
    </linearGradient>

    <!-- Comfort zone gradient (Green) -->
    <linearGradient id="comfortGradient" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#34D399"/>
      <stop offset="50%" stop-color="#10B981"/>
      <stop offset="100%" stop-color="#059669"/>
    </linearGradient>

    <!-- Hot zone gradient (Red) -->
    <linearGradient id="hotGradient" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#F87171"/>
      <stop offset="50%" stop-color="#EF4444"/>
      <stop offset="100%" stop-color="#DC2626"/>
    </linearGradient>

    <!-- Background track gradient -->
    <linearGradient id="trackGradient" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#F3F4F6"/>
      <stop offset="50%" stop-color="#E5E7EB"/>
      <stop offset="100%" stop-color="#D1D5DB"/>
    </linearGradient>

    <!-- ========================================= -->
    <!-- CLIP PATHS -->
    <!-- ========================================= -->

    <!-- Clip path for rounded progress bar ends -->
    <clipPath id="progressBarClip">
      <rect x="50" y="230" width="460" height="35" rx="18" ry="18"/>
    </clipPath>

  </defs>

  <!-- Background -->
  <rect width="1123" height="794" fill="#FFFFFF"/>

  <!-- ========================================= -->
  <!-- HEADER SECTION -->
  <!-- ========================================= -->
  <g id="header">
    <rect width="1123" height="110" fill="{{header_bg}}"/>
    <image href="{{logo_url}}" x="45" y="15" width="105" height="105" preserveAspectRatio="xMidYMid meet"/>
    <text x="190" y="45" fill="{{header_text_color}}" font-size="24" font-family="Inter, Arial, sans-serif" font-weight="700">
      {{header_title}}
    </text>
    <text x="190" y="80" fill="{{header_subtitle_color}}" font-size="16" font-family="Inter, Arial, sans-serif" font-weight="400">
      {{header_subtitle}}
    </text>
  </g>

  <!-- ========================================= -->
  <!-- CONTENT AREA (2-Column Layout) -->
  <!-- ========================================= -->

  <!-- LEFT COLUMN -->
  <g id="left-column">
    <!-- Report Info Bar (Improved) -->
    <g id="report-info" role="region" aria-label="Report Information">
      <rect x="30" y="130" width="520" height="48" class="card-elevated" rx="6" ry="6"/>

      <!-- Building Name Column -->
      <g class="info-column" transform="translate(50, 130)">
        <text y="18" font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#6B7280" letter-spacing="0.5">Building Name</text>
        <text y="36" font-family="Inter, sans-serif" font-size="13" font-weight="600" fill="#111827">{{building_name}}</text>
      </g>

      <!-- Date Range Column -->
      <g class="info-column" transform="translate(230, 130)">
        <text y="18" font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#6B7280" letter-spacing="0.5">Report Period</text>
        <text y="36" font-family="Inter, sans-serif" font-size="13" font-weight="600" fill="#111827">{{date_range}}</text>
      </g>

      <!-- Sensors Column -->
      <g class="info-column" transform="translate(420, 130)">
        <text y="18" font-family="Inter, sans-serif" font-size="9" font-weight="600" fill="#6B7280" letter-spacing="0.5">Sensors</text>
        <text y="38" font-family="Inter, sans-serif" font-size="16" font-weight="700" fill="#111827">{{total_sensors}} / <tspan fill="{{accent_primary}}">{{active_sensors}}</tspan></text>
      </g>
    </g>

    <!-- Temperature Distribution Indicator (Improved) -->
    <g id="temperature-comfort-indicator" role="figure" aria-label="Temperature Distribution Chart">
      <!-- Card Container with Elevation -->
      <rect x="30" y="190" width="520" height="380" class="card-elevated" rx="8" ry="8"/>

      <!-- Section Header with Icon -->
      <g id="chart-header" transform="translate(50, 218)">
        <text font-family="Inter, sans-serif" font-size="13" font-weight="700" fill="#1F2937">
          <tspan fill="#EF4444">🌡️</tspan>
          <tspan dx="6">Temperature Distribution</tspan>
          <tspan dx="8" font-size="11" font-weight="500" fill="#6B7280">(100%)</tspan>
        </text>
      </g>

      <!-- Progress Bar Container -->
      <g id="progress-bar-container">

        <!-- Background Track with Gradient -->
        <rect
          x="50" y="230"
          width="460" height="35"
          fill="url(#trackGradient)"
          rx="18" ry="18"
          opacity="0.7"
        />

        <!-- Segmented Progress Bar (Clipped for Perfect Rounded Ends) -->
        <g clip-path="url(#progressBarClip)">

          <!-- Cold Zone Segment (Blue) - Left Side -->
          <rect
            id="cold-segment"
            x="50" y="230"
            width="{{cold_bar_width_landscape}}" height="35"
            fill="url(#coldGradient)"
          >
            <animate
              attributeName="width"
              from="0" to="{{cold_bar_width_landscape}}"
              dur="0.7s"
              fill="freeze"
              calcMode="spline"
              keySplines="0.25 0.1 0.25 1"
            />
          </rect>

          <!-- Comfort Zone Segment (Green) - Center -->
          <rect
            id="comfort-segment"
            x="{{comfort_bar_start_landscape}}" y="230"
            width="{{comfort_bar_width_landscape}}" height="35"
            fill="url(#comfortGradient)"
          >
            <animate
              attributeName="width"
              from="0" to="{{comfort_bar_width_landscape}}"
              dur="0.7s"
              begin="0.15s"
              fill="freeze"
              calcMode="spline"
              keySplines="0.25 0.1 0.25 1"
            />
          </rect>

          <!-- Hot Zone Segment (Red) - Right Side -->
          <rect
            id="hot-segment"
            x="{{hot_bar_start_landscape}}" y="230"
            width="{{hot_bar_width_landscape}}" height="35"
            fill="url(#hotGradient)"
          >
            <animate
              attributeName="width"
              from="0" to="{{hot_bar_width_landscape}}"
              dur="0.7s"
              begin="0.3s"
              fill="freeze"
              calcMode="spline"
              keySplines="0.25 0.1 0.25 1"
            />
          </rect>

          <!-- Glass Overlay Effect -->
          <rect x="50" y="230" width="460" height="35" fill="url(#glassOverlay)" pointer-events="none"/>

        </g>

        <!-- Subtle Segment Dividers -->
        <g id="segment-dividers" opacity="0.4">
          <line x1="{{comfort_bar_start_landscape}}" y1="232" x2="{{comfort_bar_start_landscape}}" y2="263" stroke="#FFFFFF" stroke-width="1.5"/>
          <line x1="{{hot_bar_start_landscape}}" y1="232" x2="{{hot_bar_start_landscape}}" y2="263" stroke="#FFFFFF" stroke-width="1.5"/>
        </g>

      </g>

      <!-- Percentage Labels with Background Pills -->
      <g id="percentage-labels" font-family="Inter, sans-serif">

        <!-- Cold Percentage Label -->
        <g id="cold-label" transform="translate({{cold_label_x_landscape}}, 247)" opacity="0">
          <rect x="-22" y="-12" width="44" height="20" rx="10" fill="rgba(0,0,0,0.25)"/>
          <text
            text-anchor="middle"
            font-size="12"
            font-weight="700"
            fill="#FFFFFF"
            dominant-baseline="middle"
            y="1"
          >{{cold_percentage}}%</text>
          <animate attributeName="opacity" from="0" to="1" dur="0.4s" begin="0.7s" fill="freeze"/>
        </g>

        <!-- Comfort Percentage Label (Emphasized) -->
        <g id="comfort-label" transform="translate({{comfort_label_x_landscape}}, 247)" opacity="0">
          <rect x="-26" y="-13" width="52" height="22" rx="11" fill="rgba(0,0,0,0.3)"/>
          <text
            text-anchor="middle"
            font-size="14"
            font-weight="800"
            fill="#FFFFFF"
            dominant-baseline="middle"
            y="1"
          >{{comfort_percentage}}%</text>
          <animate attributeName="opacity" from="0" to="1" dur="0.4s" begin="0.85s" fill="freeze"/>
        </g>

        <!-- Hot Percentage Label -->
        <g id="hot-label" transform="translate({{hot_label_x_landscape}}, 247)" opacity="0">
          <rect x="-22" y="-12" width="44" height="20" rx="10" fill="rgba(0,0,0,0.25)"/>
          <text
            text-anchor="middle"
            font-size="12"
            font-weight="700"
            fill="#FFFFFF"
            dominant-baseline="middle"
            y="1"
          >{{hot_percentage}}%</text>
          <animate attributeName="opacity" from="0" to="1" dur="0.4s" begin="1s" fill="freeze"/>
        </g>

      </g>

      <!-- Legend with Colored Badge Indicators -->
      <g id="temperature-legend" transform="translate(50, 285)" font-family="Inter, sans-serif">

        <!-- Cold Zone Legend Item -->
        <g class="legend-item" transform="translate(0, 0)">
          <rect x="-4" y="-11" width="110" height="22" rx="11" fill="#EFF6FF" stroke="#BFDBFE" stroke-width="0.5"/>
          <circle cx="10" cy="0" r="6" fill="url(#coldGradient)"/>
          <text x="22" y="4" font-size="10" font-weight="600" fill="#1E40AF">Cold (&lt;20°C)</text>
        </g>

        <!-- Comfort Zone Legend Item -->
        <g class="legend-item" transform="translate(155, 0)">
          <rect x="-4" y="-11" width="145" height="22" rx="11" fill="#ECFDF5" stroke="#A7F3D0" stroke-width="0.5"/>
          <circle cx="10" cy="0" r="6" fill="url(#comfortGradient)"/>
          <text x="22" y="4" font-size="10" font-weight="600" fill="#065F46">Comfort (20-26°C)</text>
        </g>

        <!-- Hot Zone Legend Item -->
        <g class="legend-item" transform="translate(340, 0)">
          <rect x="-4" y="-11" width="110" height="22" rx="11" fill="#FEF2F2" stroke="#FECACA" stroke-width="0.5"/>
          <circle cx="10" cy="0" r="6" fill="url(#hotGradient)"/>
          <text x="22" y="4" font-size="10" font-weight="600" fill="#991B1B">Hot (&gt;26°C)</text>
        </g>

      </g>

      <!-- Temperature Range Info (Improved Card) -->
      <g id="temp-range-info" transform="translate(50, 325)">
        <rect x="0" y="0" width="460" height="215" rx="6" fill="#F9FAFB" stroke="#E5E7EB" stroke-width="0.5"/>

        <!-- Title -->
        <text x="15" y="25" font-family="Inter, sans-serif" font-size="11" font-weight="600" fill="#6B7280" letter-spacing="0.5">ZONE DISTRIBUTION</text>

        <line x1="15" y1="35" x2="445" y2="35" stroke="#E5E7EB" stroke-width="1"/>

        <!-- Comfort Zone Row -->
        <g transform="translate(15, 60)">
          <rect x="-5" y="-16" width="450" height="28" rx="4" fill="#ECFDF5" opacity="0.5"/>
          <circle cx="8" cy="-2" r="5" fill="url(#comfortGradient)"/>
          <text x="22" y="2" font-family="Inter, sans-serif" font-size="11" font-weight="600" fill="#065F46">Comfort (20-26°C)</text>
          <text x="420" y="2" font-family="Inter, sans-serif" font-size="14" font-weight="700" fill="#059669" text-anchor="end">{{comfort_percentage}}%</text>
        </g>

        <!-- Cold Zone Row -->
        <g transform="translate(15, 100)">
          <rect x="-5" y="-16" width="450" height="28" rx="4" fill="#EFF6FF" opacity="0.5"/>
          <circle cx="8" cy="-2" r="5" fill="url(#coldGradient)"/>
          <text x="22" y="2" font-family="Inter, sans-serif" font-size="11" font-weight="600" fill="#1E40AF">Cold (&lt;20°C)</text>
          <text x="420" y="2" font-family="Inter, sans-serif" font-size="14" font-weight="700" fill="#2563EB" text-anchor="end">{{cold_percentage}}%</text>
        </g>

        <!-- Hot Zone Row -->
        <g transform="translate(15, 140)">
          <rect x="-5" y="-16" width="450" height="28" rx="4" fill="#FEF2F2" opacity="0.5"/>
          <circle cx="8" cy="-2" r="5" fill="url(#hotGradient)"/>
          <text x="22" y="2" font-family="Inter, sans-serif" font-size="11" font-weight="600" fill="#991B1B">Hot (&gt;26°C)</text>
          <text x="420" y="2" font-family="Inter, sans-serif" font-size="14" font-weight="700" fill="#DC2626" text-anchor="end">{{hot_percentage}}%</text>
        </g>
      </g>
    </g>
  </g>

  <!-- RIGHT COLUMN -->
  <g id="right-column">
    <!-- Temperature Analysis Chart (Improved) -->
    <g id="chart-section">
      <text x="570" y="145" class="section-header">Temperature Analysis: Hotspots and Cold Zones</text>

      <rect x="570" y="155" width="523" height="250" class="card-elevated" rx="8"/>

      <!-- Two Column Layout: Left 40% (Thermometer) | Right 60% (Chart + Legend) -->

      <!-- LEFT COLUMN: Thermometer Graphic (40% = ~209px) -->
      <g id="thermometer-section" transform="translate(580, 165)">
        <!-- Thermometer Image -->
        <image href="{{thermometer_url}}" x="20" y="5" width="80" height="180" preserveAspectRatio="xMidYMid meet"/>

        <!-- Temperature Values Display with Colored Boxes -->

        <!-- Max Temperature (Top - Red/Hot) -->
        <g id="max-temp-display" transform="translate(110, 25)">
          <rect x="-6" y="-14" width="65" height="38" rx="5" fill="#FEF2F2" stroke="#FECACA" stroke-width="0.5"/>
          <text x="0" y="0" font-family="Inter, sans-serif" font-size="8" font-weight="700" fill="#DC2626" letter-spacing="0.5">MAX</text>
          <text x="0" y="16" font-family="Inter, sans-serif" font-size="16" font-weight="700" fill="#DC2626">{{overall_max_temp}}°C</text>
        </g>

        <!-- Average Temperature (Middle - Amber) -->
        <g id="avg-temp-display" transform="translate(110, 85)">
          <rect x="-6" y="-14" width="65" height="38" rx="5" fill="#FFFBEB" stroke="#FDE68A" stroke-width="0.5"/>
          <text x="0" y="0" font-family="Inter, sans-serif" font-size="8" font-weight="700" fill="#D97706" letter-spacing="0.5">AVG</text>
          <text x="0" y="16" font-family="Inter, sans-serif" font-size="14" font-weight="700" fill="#D97706">{{overall_avg_temp}}°C</text>
        </g>

        <!-- Min Temperature (Bottom - Blue/Cold) -->
        <g id="min-temp-display" transform="translate(110, 145)">
          <rect x="-6" y="-14" width="65" height="38" rx="5" fill="#EFF6FF" stroke="#BFDBFE" stroke-width="0.5"/>
          <text x="0" y="0" font-family="Inter, sans-serif" font-size="8" font-weight="700" fill="#2563EB" letter-spacing="0.5">MIN</text>
          <text x="0" y="16" font-family="Inter, sans-serif" font-size="16" font-weight="700" fill="#2563EB">{{overall_min_temp}}°C</text>
        </g>
      </g>

      <!-- RIGHT COLUMN: Chart + Legend (60% = ~314px) -->
      <g id="chart-column" transform="translate(779, 155)">
        <!-- Chart Legend with Badge Style -->
        <g id="chart-legend" font-family="Inter, sans-serif">
          <g transform="translate(0, 0)">
            <rect x="-4" y="-11" width="85" height="22" rx="11" fill="#FEF2F2" stroke="#FECACA" stroke-width="0.5"/>
            <rect x="4" y="-3" width="8" height="8" rx="2" fill="#EF4444"/>
            <text x="18" y="4" font-size="9" font-weight="600" fill="#991B1B">Hotspots</text>
          </g>
          <g transform="translate(95, 0)">
            <rect x="-4" y="-11" width="95" height="22" rx="11" fill="#EFF6FF" stroke="#BFDBFE" stroke-width="0.5"/>
            <rect x="4" y="-3" width="8" height="8" rx="2" fill="#3B82F6"/>
            <text x="18" y="4" font-size="9" font-weight="600" fill="#1E40AF">Cold Zones</text>
          </g>
          <g transform="translate(200, 0)">
            <rect x="-4" y="-11" width="70" height="22" rx="11" fill="#F9FAFB" stroke="#E5E7EB" stroke-width="0.5"/>
            <line x1="4" y1="1" x2="16" y2="1" stroke="#9CA3AF" stroke-width="2" stroke-dasharray="3,2"/>
            <text x="22" y="4" font-size="9" font-weight="600" fill="#6B7280">Avg</text>
          </g>
        </g>

        <!-- Chart Placeholder -->
        <foreignObject x="0" y="30" width="304" height="210">
          <div xmlns="http://www.w3.org/1999/xhtml" id="chart-container" style="width:100%; height:100%;">
            <canvas id="temperature-comparison-chart"></canvas>
          </div>
        </foreignObject>
      </g>
    </g>

    <!-- Sensor Details Section (Improved) -->
    <g id="hotspots-coldzones">
      <text x="570" y="435" class="section-header">Sensor Temperature Details</text>

      <!-- Hotspots Table (Top 3 Hottest) -->
      <rect x="570" y="445" width="523" height="120" class="card-hotspot" rx="8"/>

      <!-- Table Title -->
      <g transform="translate(585, 465)">
        <text font-family="Inter, sans-serif" font-size="11" font-weight="700" fill="#FCA5A5">
          <tspan fill="#EF4444">🔥</tspan>
          <tspan dx="4">Hotspots</tspan>
          <tspan dx="6" font-size="9" font-weight="500" fill="#94A3B8">(Top 3 Highest Temperature)</tspan>
        </text>
      </g>

      <!-- Hotspot Rows with Rank Badges -->
      <g transform="translate(0, 488)" font-family="Inter, sans-serif">
        <rect x="580" y="-12" width="24" height="18" rx="4" fill="#EF4444" opacity="0.2"/>
        <text x="592" fill="#FFFFFF" font-size="11" font-weight="700" text-anchor="middle">#1</text>
        <text x="615" fill="#E2E8F0" font-size="11" font-weight="600">{{hotspot_1_name}}</text>
        <text x="750" fill="#FFFFFF" font-size="16" font-weight="700">{{hotspot_1_avg}}<tspan fill="#94A3B8" font-size="11">°C</tspan></text>
        <text x="850" fill="#94A3B8" font-size="9">Min: {{hotspot_1_min}}°C | Max: {{hotspot_1_max}}°C</text>
        <text x="1050" fill="#EF4444" font-size="10" font-weight="600">+{{hotspot_1_deviation}}°C</text>
      </g>

      <g transform="translate(0, 513)" font-family="Inter, sans-serif">
        <rect x="580" y="-12" width="24" height="18" rx="4" fill="#EF4444" opacity="0.15"/>
        <text x="592" fill="#FCA5A5" font-size="11" font-weight="600" text-anchor="middle">#2</text>
        <text x="615" fill="#E2E8F0" font-size="11" font-weight="600">{{hotspot_2_name}}</text>
        <text x="750" fill="#FFFFFF" font-size="16" font-weight="700">{{hotspot_2_avg}}<tspan fill="#94A3B8" font-size="11">°C</tspan></text>
        <text x="850" fill="#94A3B8" font-size="9">Min: {{hotspot_2_min}}°C | Max: {{hotspot_2_max}}°C</text>
        <text x="1050" fill="#EF4444" font-size="10" font-weight="600">+{{hotspot_2_deviation}}°C</text>
      </g>

      <g transform="translate(0, 538)" font-family="Inter, sans-serif">
        <rect x="580" y="-12" width="24" height="18" rx="4" fill="#EF4444" opacity="0.1"/>
        <text x="592" fill="#F87171" font-size="11" font-weight="600" text-anchor="middle">#3</text>
        <text x="615" fill="#E2E8F0" font-size="11" font-weight="600">{{hotspot_3_name}}</text>
        <text x="750" fill="#FFFFFF" font-size="16" font-weight="700">{{hotspot_3_avg}}<tspan fill="#94A3B8" font-size="11">°C</tspan></text>
        <text x="850" fill="#94A3B8" font-size="9">Min: {{hotspot_3_min}}°C | Max: {{hotspot_3_max}}°C</text>
        <text x="1050" fill="#EF4444" font-size="10" font-weight="600">+{{hotspot_3_deviation}}°C</text>
      </g>

      <!-- Cold Zones Table (Top 3 Coldest) -->
      <rect x="570" y="575" width="523" height="120" class="card-coldzone" rx="8"/>

      <!-- Table Title -->
      <g transform="translate(585, 595)">
        <text font-family="Inter, sans-serif" font-size="11" font-weight="700" fill="#93C5FD">
          <tspan fill="#3B82F6">❄️</tspan>
          <tspan dx="4">Cold Zones</tspan>
          <tspan dx="6" font-size="9" font-weight="500" fill="#94A3B8">(Top 3 Lowest Temperature)</tspan>
        </text>
      </g>

      <!-- Cold Zone Rows with Rank Badges -->
      <g transform="translate(0, 618)" font-family="Inter, sans-serif">
        <rect x="580" y="-12" width="24" height="18" rx="4" fill="#3B82F6" opacity="0.2"/>
        <text x="592" fill="#FFFFFF" font-size="11" font-weight="700" text-anchor="middle">#1</text>
        <text x="615" fill="#E2E8F0" font-size="11" font-weight="600">{{coldzone_1_name}}</text>
        <text x="750" fill="#FFFFFF" font-size="16" font-weight="700">{{coldzone_1_avg}}<tspan fill="#94A3B8" font-size="11">°C</tspan></text>
        <text x="850" fill="#94A3B8" font-size="9">Min: {{coldzone_1_min}}°C | Max: {{coldzone_1_max}}°C</text>
        <text x="1050" fill="#3B82F6" font-size="10" font-weight="600">{{coldzone_1_deviation}}°C</text>
      </g>

      <g transform="translate(0, 643)" font-family="Inter, sans-serif">
        <rect x="580" y="-12" width="24" height="18" rx="4" fill="#3B82F6" opacity="0.15"/>
        <text x="592" fill="#93C5FD" font-size="11" font-weight="600" text-anchor="middle">#2</text>
        <text x="615" fill="#E2E8F0" font-size="11" font-weight="600">{{coldzone_2_name}}</text>
        <text x="750" fill="#FFFFFF" font-size="16" font-weight="700">{{coldzone_2_avg}}<tspan fill="#94A3B8" font-size="11">°C</tspan></text>
        <text x="850" fill="#94A3B8" font-size="9">Min: {{coldzone_2_min}}°C | Max: {{coldzone_2_max}}°C</text>
        <text x="1050" fill="#3B82F6" font-size="10" font-weight="600">{{coldzone_2_deviation}}°C</text>
      </g>

      <g transform="translate(0, 668)" font-family="Inter, sans-serif">
        <rect x="580" y="-12" width="24" height="18" rx="4" fill="#3B82F6" opacity="0.1"/>
        <text x="592" fill="#60A5FA" font-size="11" font-weight="600" text-anchor="middle">#3</text>
        <text x="615" fill="#E2E8F0" font-size="11" font-weight="600">{{coldzone_3_name}}</text>
        <text x="750" fill="#FFFFFF" font-size="16" font-weight="700">{{coldzone_3_avg}}<tspan fill="#94A3B8" font-size="11">°C</tspan></text>
        <text x="850" fill="#94A3B8" font-size="9">Min: {{coldzone_3_min}}°C | Max: {{coldzone_3_max}}°C</text>
        <text x="1050" fill="#3B82F6" font-size="10" font-weight="600">{{coldzone_3_deviation}}°C</text>
      </g>
    </g>
  </g>

  <!-- ========================================= -->
  <!-- FOOTER SECTION -->
  <!-- ========================================= -->
  <g id="footer">
    <line x1="30" y1="725" x2="1093" y2="725" stroke="{{footer_bg}}" stroke-width="2"/>
    <rect y="733" width="1123" height="61" fill="{{footer_bg}}"/>
    <text x="40" y="767" fill="{{footer_text_color}}" font-size="12" font-family="Inter, Arial, sans-serif">
      {{footer_text}}
    </text>
    <text x="1083" y="767" fill="{{footer_date_color}}" font-size="12" font-family="Inter, Arial, sans-serif" text-anchor="end">
      {{generation_date}}
    </text>
  </g>
</svg>
